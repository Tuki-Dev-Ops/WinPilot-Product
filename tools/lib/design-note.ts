/**
 * 화면별 **설계 배경** — 왜 지금의 구조인가.
 *
 * ## 왜 명세를 그대로 쓰지 않나
 * 화면 명세의 배경은 만드는 사람끼리 읽는 글이라 겪은 일을 이야기로 적는다 —
 * `표 한 장이 전부였습니다. 회사명과 대표가 일곱 줄로 서 있었는데…`. 발주처가 읽는 문서에서
 * 그 문장은 **무엇이 결정되었는지를 알려 주지 않는다.** 읽는 사람이 이야기에서 결론을 스스로
 * 뽑아내야 한다.
 *
 * 여기서는 같은 내용을 **판단과 근거**로 다시 적는다 — 무엇을 어떻게 구성했고, 그렇게 정한
 * 이유가 무엇인가. 원본의 판단을 바꾸지 않는다. 바꾸는 것은 말하는 방식뿐이다.
 *
 * ## 없으면 세우지 않는다
 * 배경이 적히지 않은 화면에는 이 칸이 서지 않는다. 지어내면 그 문장이 설계 근거로 굳는다.
 */

/** `프로젝트 slug + 기능 ID` 로 찾는다. 두 프로젝트에 같은 ID 가 있을 수 있다. */
export const DESIGN_NOTE: Record<string, string> = {
  /* ── F&B ─────────────────────────────────────────────────── */

  'FnB/MARKETING-001':
    '본사 마케팅 활동을 별도 화면으로 분리하여 제공한다. 창업 안내 문서가 항목별로 동일한 내용을 서술하고 있어, 두 곳에서 서로 다른 값이 유지될 위험이 있었다. 실제 게시물을 원본으로 삼아 단일 출처를 확보한다.',

  'FnB/STORE-001':
    '지역 필터를 행정 경계 3단계(시·도 → 시·군·구 → 읍·면·동)가 아닌 단일 지역 목록으로 구성한다. 방문자가 필요로 하는 것은 행정 구분이 아니라 매장의 위치이며, 지역 목록은 가맹점 데이터에서 도출하여 별도 관리 대상을 만들지 않는다.',

  'FnB/FRANCHISE-001':
    '창업 안내를 비용 · 절차 · 자주 묻는 질문 세 화면으로 분리하고, 본 화면은 비용과 절차를 담당한다. 재방문자가 질문 영역에 도달하기 위해 비용 표와 절차를 다시 지나야 하는 구조를 해소하기 위한 것이다.',

  'FnB/FRANCHISE-002':
    '창업 안내 하위 화면 간 이동은 상단 탭으로 일원화한다. 별도의 복귀 링크를 함께 두면 동일한 이동 수단이 두 개가 되어 사용자가 어느 것을 선택해야 하는지 판단해야 한다.',

  'FnB/FRANCHISE-003':
    '자주 묻는 질문을 창업 안내에서 분리하여 독립 화면으로 구성한다. 질문 확인이 목적인 재방문자가 비용 표와 절차를 경유하지 않고 직접 접근할 수 있도록 한다.',

  /* ── IR ──────────────────────────────────────────────────── */

  'IR/COMPANY-001':
    '회사 소개를 사업자 정보 표가 아닌 서술형 구조로 구성한다. 상호 · 대표자 · 사업자등록번호는 거래 확인 목적의 값이므로 푸터에서 제공하고, 본 화면은 지향점 · 핵심 문장 · 수행 업무 · 사업 영역 순으로 배치하여 첫 방문자가 회사의 사업 정체성을 파악할 수 있도록 한다.',

  'IR/HISTORY-001':
    '연혁을 연도 단위가 아닌 다년 단위로 묶어 배치한다. 연도별로 제목을 세우면 항목이 하나인 연도에도 제목이 동일한 비중을 차지하여, 이력이 아니라 제목 목록으로 읽힌다. 좌측 기간 제목이 우측의 복수 항목을 거느리는 구조로 정보 밀도를 조정한다.',

  'IR/CERT-001':
    '특허와 인증을 회사 소개 내 서술 문단이 아닌 독립 목록 화면으로 분리한다. 이 정보의 주된 이용 목적은 제시받은 등록번호의 대조 확인이므로, 등록번호 · 발급 기관 · 취득일이 항목별로 정렬된 구조가 필요하다.',

  'IR/CERT-002':
    '개별 인증 항목에 고유 URL 을 부여하여 직접 접근과 공유가 가능하도록 구성한다. 목록 화면 주소만 제공되면 수신자가 목록에서 해당 항목을 다시 탐색해야 한다.',

  'IR/SOLUTION-001':
    '인력이 수행하는 서비스를 제품과 분리하여 독립 화면으로 제공한다. 서비스는 계약 즉시 사용하는 제품과 제공 방식이 상이하므로, 제품 카드와 동일한 지면에 배치하면 도입 단위와 과금 방식에 대한 오해가 발생한다.',

  'IR/SOLUTION-002':
    '인프라 서비스를 제품 목록이 아닌 전용 화면으로 연결한다. 인프라 검토 목적으로 진입한 방문자가 클라우드 제품 목록에 도달하면 탐색 경로를 잘못 선택한 것으로 인식하게 된다.',

  'IR/PRODUCT-001':
    '제품 목록 화면에 전체 라인업을 구성한다. 상단 내비게이션과 홈의 주요 진입 경로가 모두 이 화면으로 연결되어 있어, 서비스 내에서 방문 비중이 가장 높은 화면에 해당한다.',

  'IR/SOLUTION-003':
    '제품별 상세 화면을 제공하여 해결 대상 문제와 해결 방식을 서술한다. 목록의 카드 한 장으로는 도입 판단에 필요한 정보가 담기지 않아, 검토자가 영업 담당자에게 별도 문의해야 하는 구조가 된다.',

  'IR/SOLUTION-004':
    '제품별 상세 화면을 제공하여 해결 대상 문제와 해결 방식을 서술한다. 부서별로 분산된 데이터 관리 문제는 한 줄 소개로 전달되지 않으므로, 문제 정의와 해결 방식을 분리하여 제시한다.',

  'IR/SOLUTION-005':
    '제품별 상세 화면을 제공하여 해결 대상 문제와 해결 방식을 서술한다. 담당자 변경 시 고객 이력이 유실되는 문제와, 일반적인 영업 관리 도구와의 차이를 설명할 지면이 필요하다.',

  'IR/SOLUTION-006':
    '제품 넷이 모두 동일한 구조의 상세 화면을 갖도록 구성한다. 동일 라인업 중 하나만 다른 화면으로 연결되면 해당 제품이 아직 제공되지 않는 것으로 인식된다.',

  'IR/INQUIRY-001':
    '문의 유형 선택을 포함한 접수 양식을 제공한다. 단일 이메일 주소로 문의를 접수하면 수신 측에서 유형별 분류와 담당 배정이 불가능하고, 발신자도 어떤 정보를 제공해야 하는지 판단하기 어렵다.',

  'IR/NOTICE-001':
    '공지를 목록 화면으로 제공하여 게시 이후에도 재확인이 가능하도록 한다. 팝업만으로 안내하면 닫은 이후 해당 내용에 접근할 경로가 없다.',

  'IR/NEWS-001':
    '소식을 전체 목록 화면으로 제공한다. 홈의 요약 영역은 최신 항목만 노출하고 가로 스크롤을 요구하므로, 지난 소식을 탐색하는 목적에는 적합하지 않다.',

  'IR/FAQ-001':
    '반복 접수되는 질문을 사전 답변으로 제공하여 문의 접수량을 감축한다. 동일한 질문에 대해 매번 개별 응대하는 구조는 응대 비용과 응답 대기 시간을 함께 발생시킨다.',

  'IR/SUPPORT-001':
    '주소 · 연락처와 함께 지도 및 교통편 안내를 제공한다. 푸터의 주소 표기만으로는 방문 소요 시간과 주차 가능 여부 등 방문 준비에 필요한 정보가 전달되지 않는다.',

  'IR/HOME-001':
    '홈 화면을 회사 소개 중심으로 구성하고 수치 정보는 후순위로 배치한다. 상장사 홈페이지의 관례에 따라 공시 · 주가 · 일정 요약을 전면에 배치할 경우, 회사를 처음 접하는 방문자가 사업 내용을 파악하기 전에 수치 표를 먼저 마주하게 된다.',
};
