import { writeFileSync } from 'node:fs';
import { COMMON_NON_FUNCTIONAL, SCREENS } from './lib/fnb-model';
import { DATA_POLICY, ERRORS, ROLES } from './lib/fnb-backend';
import { esc, rich } from './lib/html';
import { formal } from './lib/polite';
import { shell } from './lib/page';

/**
 * **비기능 명세서** — 얼마나 잘 도는가.
 *
 * ## 왜 갈라 냈나
 * 기능 명세서에 함께 두었더니 화면마다 「비기능 요건」 절이 붙어, 같은 요건이 47벌로 흩어졌다.
 * "모션 감소를 켠 사용자에게 어떻게 도는가" 를 확인하려면 47개 절을 훑어야 한다.
 *
 * 비기능은 **화면이 아니라 주제로 묻는다.** 성능 · 반응형 · 접근성 · 보안 · 데이터 · 운영으로
 * 나누고, 그 화면에만 걸리는 것만 화면별 절에 남긴다.
 *
 * ## 판정 가능하게 적는다
 * "빠르게 동작한다" 는 비기능 요건이 아니다. 검수 자리에서 통과와 미통과를 가를 수 없기
 * 때문이다. 재는 방법과 기준값을 함께 적고, 저장소의 검사 도구로 재는 것은 그 도구를 적는다.
 *
 * 기준값은 **실제로 재어 정한 것만** 적는다. 지어낸 수를 적어 두면 그것이 그대로 계약 조건이
 * 된다.
 *
 * ```
 * pnpm nfr:build
 * ```
 */

const cells = (...values: string[]): string => `<tr>${values.map((one) => `<td>${one}</td>`).join('')}</tr>`;

const table = (head: string, rows: string, klass = ''): string => `<table class="${klass}">
  <thead><tr>${head}</tr></thead>
  <tbody>${rows}</tbody>
</table>`;

const th = (label: string, width?: number): string =>
  `<th${width ? ` style="width:${width}px"` : ''}>${esc(label)}</th>`;

const bullets = (items: readonly string[]): string =>
  `<ul>${items.map((one) => `<li>${rich(one)}</li>`).join('')}</ul>`;

const grouped = (list: readonly (readonly string[])[]): string =>
  list
    .map((row, index) => {
      const [area, ...rest] = row;
      const first = list[index - 1]?.[0] !== area;
      let span = 0;
      while (list[index + span]?.[0] === area) span += 1;
      const head = first ? `<td class="d1" rowspan="${span}">${esc(area ?? '')}</td>` : '';
      return `<tr${first ? ' class="head"' : ''}>${head}${rest.map((one) => `<td class="memo">${rich(one)}</td>`).join('')}</tr>`;
    })
    .join('');

/* ── 비기능 요건 ───────────────────────────────────────────── */

/**
 * 주제별 요건. 「검증 방법」 칸이 이 문서의 핵심이다.
 *
 * 재는 방법이 없는 요건은 검수 자리에서 통과와 미통과를 가를 수 없고, 그러면 인수는 결국
 * 인상으로 판정된다. 저장소에 검사 도구가 있는 것은 그 도구 이름을 적는다 — 사람이 매번
 * 눈으로 확인하는 것보다 낫다.
 */
const NFR: [string, string, string, string][] = [
  [
    '성능',
    '화면 하나가 내려받는 자원 크기를 예산 안으로 관리한다.',
    'JavaScript 700KB · CSS 120KB · 이미지 400KB (전송 기준). 문서 화면은 JavaScript 1600KB 를 적용한다.',
    '저장소의 `weight:check` 가 전 화면을 실제로 열어 전송 바이트를 잰다.',
  ],
  [
    '성능',
    '목록 화면은 조회 조건이 바뀔 때 화면 전체를 다시 그리지 않는다.',
    '조건 변경 시 목록 영역만 갱신되며, 헤더 · 필터 상태는 유지된다.',
    '화면별 검수 기준으로 확인한다.',
  ],
  [
    '성능',
    '지도는 매장 수와 무관하게 좌표 변환 요청을 발생시키지 않는다.',
    '좌표는 가맹점 데이터가 보유한다. 화면에서 주소를 좌표로 변환하지 않는다.',
    '네트워크 요청 확인 — 지도 SDK 외 외부 호출이 없어야 한다.',
  ],
  [
    '반응형',
    '정의된 기준 해상도에서 의도하지 않은 가로 스크롤이 발생하지 않는다.',
    '1280 · 1024 · 768 · 390 네 너비. 화면 **47개 × 4** 지점을 모두 검사한다.',
    '저장소의 `overflow:check` 가 자동으로 검사한다.',
  ],
  [
    '반응형',
    '표와 넓은 콘텐츠는 화면 밖으로 넘치지 않고 자체 영역에서 스크롤한다.',
    '좁은 너비에서 표는 가로 스크롤 영역을 가지며, 페이지 본문은 가로로 움직이지 않는다.',
    '`overflow:check` · 육안 확인.',
  ],
  [
    '접근성',
    '색만으로 상태를 알리지 않는다.',
    '공개 · 비공개 등 상태 표시는 색과 함께 **모양 또는 글자**를 둔다.',
    '상태 표시 요소를 흑백으로 두고 구분 가능한지 확인한다.',
    ],
  [
    '접근성',
    '모션 감소를 켠 사용자에게는 자동 전환이 동작하지 않는다.',
    '캐러셀 자동 넘김과 흐르는 띠가 멈춘다. 사용자가 직접 조작하는 이동은 유지한다.',
    '운영체제의 모션 줄이기 설정을 켜고 확인한다.',
  ],
  [
    '접근성',
    '자동 전환은 사용자 조작 중에 멈춘다.',
    '마우스가 올라가거나 포커스가 들어오거나 직접 밀면 자동 넘김이 멈춘다.',
    '화면별 검수 기준으로 확인한다.',
  ],
  [
    '접근성',
    '화면 밖으로 벗어난 영역의 자동 전환은 동작하지 않는다.',
    '보이지 않는 영역에서 타이머가 돌지 않는다.',
    '스크롤 후 동작 확인.',
  ],
  [
    '사용성',
    '알림은 한 자리에서만 표시한다.',
    '토스트는 화면 하단 정중앙 한 곳에서만 뜬다.',
    '전 화면 공통 확인.',
  ],
  [
    '사용성',
    '데이터가 없는 상태를 빈 화면으로 두지 않는다.',
    '목록 결과가 0건이면 빈 상태 문구를 세우고, 조건이 걸려 있으면 조건을 지우는 경로를 함께 둔다.',
    '「기능 명세서」 9장 예외 처리 기준으로 확인한다.',
  ],
  [
    '사용성',
    '입력 오류는 미입력과 형식 오류를 구분해 안내한다.',
    '「형식이 아닙니다」는 빈 칸에 대한 답이 아니다. 항목별로 사유를 구분한다.',
    '양식이 있는 화면의 검수 기준으로 확인한다.',
  ],
  [
    '호환성',
    '최신 브라우저에서 동작한다.',
    'Chrome · Edge · Safari · Firefox 의 **최신 두 개 버전**과 iOS · Android 기본 브라우저.',
    '대상 브라우저에서 주요 화면 육안 확인. **지원 범위는 발주처 확인이 필요하다.**',
  ],
  [
    '호환성',
    '이미지가 없는 상태에서도 화면 구조가 무너지지 않는다.',
    '이미지 미제공 영역은 대체 표기로 자리를 유지한다.',
    '이미지 차단 상태로 확인한다.',
  ],
  [
    '보안',
    '고객 서비스와 관리자 서비스 모두 HTTPS 로만 제공한다.',
    '창업 문의는 개인정보가 오가는 경로다.',
    '배포 환경 설정 확인. **2단계 · 배포 시점 적용.**',
  ],
  [
    '보안',
    '관리자 서비스는 인증된 운영자만 접근한다.',
    '권한 등급은 **대표 · 운영 · 조회** 세 가지이며, 등급별 연산 범위는 4장에 정리한다.',
    '**2단계 구현 대상.** 1단계에서는 화면 흐름까지 구현되어 있다.',
  ],
  [
    '보안',
    '개인정보가 포함된 데이터는 접근 권한을 분리한다.',
    '창업 문의는 메뉴 · 콘텐츠 관리와 다른 권한으로 다룬다. **분리 수준은 발주처 확인이 필요하다.**',
    '**2단계 구현 대상.**',
  ],
  [
    '보안',
    '연락처는 저장 시 암호화하고 목록에서는 일부를 가린다.',
    '**권고 사항이며 적용 여부는 발주처가 정한다.** 목록 화면은 여러 사람이 함께 보는 자리다.',
    '**2단계 구현 대상.**',
  ],
  [
    '보안',
    '화면 검증에 의존하지 않는다.',
    '「기능 명세서」에서 **서버 검증**으로 표기된 규칙은 API 직접 호출에도 적용되어야 한다.',
    '**2단계 완료 기준.** API 를 직접 호출해 확인한다.',
  ],
  [
    '데이터',
    '동일한 값을 두 곳에서 관리하지 않는다.',
    '고객 서비스와 관리자 서비스는 같은 데이터를 참조한다. 합계 · 건수 · 지역 목록은 저장하지 않고 계산한다.',
    '「기능 명세서」의 데이터 변경 영향도로 확인한다.',
  ],
  [
    '데이터',
    '노출 중단과 데이터 삭제를 구분한다.',
    '공개 · 노출 상태를 해제해도 데이터는 유지된다. 삭제는 더 이상 사용하지 않는 데이터에 한정한다.',
    '상태 변경 후 관리자 목록에 값이 남아 있는지 확인한다.',
  ],
  [
    '데이터',
    '삭제는 사본까지 함께 처리한다.',
    '본문뿐 아니라 첨부 · 검색 색인 · 캐시에 남은 사본을 함께 제거한다.',
    '**2단계 구현 대상.**',
  ],
  [
    '운영',
    '변경 이력을 남긴다.',
    '등록 · 수정 · 삭제마다 **누가 · 언제 · 무엇을** 바꿨는지 기록한다.',
    '**2단계 구현 대상.** 보존 기간은 5장 참조.',
  ],
  [
    '운영',
    '개인정보 열람 기록을 남긴다.',
    '창업 문의를 열어 본 기록을 남긴다. 변경 기록만으로는 누가 들여다봤는지 알 수 없다.',
    '**2단계 구현 대상.**',
  ],
  [
    '외부 연동',
    '지도를 이용할 수 없어도 매장 정보는 조회된다.',
    '키 미설정 · 도메인 미등록 · 쿼터 초과 시 지도 영역은 대체 안내를 표시하며, 매장 목록과 검색은 정상 동작한다.',
    '키를 비운 상태로 매장 찾기 화면을 확인한다.',
  ],
  [
    '외부 연동',
    '외부 서비스 사용량을 관리한다.',
    '카카오 지도는 개발자 계정당 앱 1개에 무료 쿼터가 부여된다. 사용량은 개발자 콘솔에서 확인한다.',
    '「요구사항 정의서」 6.1 참조. 운영 주체는 발주처다.',
  ],
  [
    '문서',
    '문서와 코드가 어긋난 상태로 남지 않는다.',
    '화면이 등록되었는데 명세가 비어 있거나, 명세가 가리키는 화면이 사라진 상태를 허용하지 않는다.',
    '`spec:check` · `sync:check` · `docs:check` · `bind:check`',
  ],
];

/* ── 화면별 비기능 ─────────────────────────────────────────── */

const screenRows = SCREENS.filter((one) => one.spec.nonFunctional.length > 0)
  .map((one) =>
    cells(
      `<code>${esc(one.featureId)}</code>`,
      esc(one.name),
      esc(one.appLabel),
      bullets(one.spec.nonFunctional.map(formal)),
    ),
  )
  .join('');

const screenTotal = SCREENS.reduce((sum, one) => sum + one.spec.nonFunctional.length, 0);

/* ── 품질 검사 ─────────────────────────────────────────────── */

const CHECKS: [string, string, string][] = [
  ['`spec:check`', '이름 · 주소 · 화면 등록부가 기능 레지스트리와 일치하는가', '1단계'],
  ['`sync:check`', '레지스트리에 적힌 컴포넌트 이름이 실제 파일에도 그대로인가', '1단계'],
  ['`docs:check`', '화면은 있는데 명세가 비어 있는 자리가 있는가', '1단계'],
  ['`bind:check`', '화면이 적어 둔 값의 출처가 아직 존재하는가', '1단계'],
  ['`overflow:check`', '네 너비에서 가로로 넘치는 곳이 있는가', '1단계'],
  ['`weight:check`', '화면 하나가 실제로 내려받는 바이트가 예산 안인가', '1단계'],
];

const body = `
<table class="kit">
  <tr><th>문서 사용 안내</th></tr>
  <tr><td>
    <ol>
      <li>이 문서는 <strong>얼마나 잘 도는가</strong>를 정의합니다. 무엇을 하는가는 「기능 명세서」에 있습니다.</li>
      <li>모든 요건에 <strong>검증 방법</strong>을 함께 적었습니다. 재는 방법이 없는 요건은 검수 자리에서 통과와 미통과를 가를 수 없어, 인수가 결국 인상으로 판정됩니다.</li>
      <li>기준값은 <strong>실제로 재어 정한 것만</strong> 적었습니다. <span class="tbd">발주처 확인이 필요한 항목은 그렇게 표시</span>했으며, 임의로 채우지 않았습니다.</li>
      <li><strong>2단계 구현 대상</strong>으로 표기한 항목은 서버 구축 시점에 적용됩니다. 1단계 인수 시점의 판정 대상이 아닙니다.</li>
      <li>본 과업의 문서는 네 벌입니다 — 요구사항 정의서 · 과업범위 정의서 · 기능 명세서 · <strong>비기능 명세서</strong>.</li>
    </ol>
  </td></tr>
</table>

<h2>1. 적 용 범 위</h2>
<p>
  본 문서는 F&amp;B 고객 서비스와 관리자 서비스 <strong>전 화면 ${SCREENS.length}개</strong>에 적용된다.
  화면 하나에만 걸리는 요건은 3장 「화면별 비기능 요건」에 따로 두었다.
</p>
<p>
  1단계(Front-End)에서 판정 가능한 요건과 2단계(Back-End)에서 판정하는 요건을 구분해 표기한다.
  구분하지 않으면 서버가 없는 상태에서 보안 요건이 미충족으로 잡히고, 실제로는 아직 만들 차례가
  아닌 것을 결함으로 다루게 된다.
</p>

<h2>2. 공 통 비 기 능 요 건</h2>
<p class="lead">
  주제별로 정리한다. 요건마다 <strong>기준값</strong>과 <strong>검증 방법</strong>을 함께 적었으며,
  저장소에 검사 도구가 있는 항목은 그 도구를 적었다.
</p>
${table(
  th('구분', 74) + th('요건') + th('기준') + th('검증 방법', 260),
  grouped(NFR),
)}

<h3>2.1 전 화면 공통 제작 기준</h3>
<p class="lead">화면을 가리지 않고 전 화면 제작에 적용되는 기준이다.</p>
${bullets(COMMON_NON_FUNCTIONAL.map(formal))}

<h2>3. 화 면 별 비 기 능 요 건</h2>
<p class="lead">
  해당 화면에만 걸리는 요건이다. 전 화면에 걸리는 것은 2장에 한 번만 적었으므로 여기서 되풀이하지
  않는다. 총 <strong>${screenTotal}항목</strong>.
</p>
${table(th('기능 ID', 88) + th('화면', 128) + th('구분', 78) + th('요건'), screenRows)}

<h2>4. 권 한 요 건</h2>
<p class="lead">
  권한 등급은 현재 데이터에 정의된 <strong>대표 · 운영 · 조회</strong> 세 등급을 사용한다.
  각 등급의 연산 범위는 현재 문서에 정의되어 있지 않으므로 아래 배분은
  <strong class="tbd">제안이며 확정은 발주처가 한다.</strong>
  실제 인증 및 접근 통제 구현은 <strong>2단계 대상</strong>이다.
</p>
${table(
  ROLES[0]!.map((one) => th(one)).join(''),
  ROLES.slice(1)
    .map((row) => cells(...row.map((one) => (one === 'O' ? '<strong>O</strong>' : esc(one)))))
    .join(''),
  'right',
)}

<h2>5. 데 이 터 보 존 및 파 기 요 건</h2>
<p class="lead">
  수집 · 보유 · 삭제 · 소멸 · 이력 · 권한 · 보안 · 백업 정책이다. 화면에 드러나지 않지만 서버
  구축 이전에 확정되어야 하는 값이므로 여기에 정의한다.
  <strong class="tbd">「권고」로 표기한 기간과 값은 확정된 것이 아니며</strong>, 근거 법령을 함께
  적었으나 실제 값은 발주처가 정한다.
</p>
${table(th('구분', 62) + th('항목', 146) + th('정의 · 기준'), grouped(DATA_POLICY))}

<h2>6. 오 류 응 답 요 건</h2>
<p class="lead">
  화면을 가리지 않고 같은 방식으로 처리한다. 화면마다 되풀이해 적으면 ${SCREENS.length}벌이 되고,
  한 곳만 고쳐진다.
</p>
${table(
  th('상황', 136) + th('발생 조건', 260) + th('시스템 처리 · 화면 표시'),
  ERRORS.map(([a, b, c]) => cells(esc(a), rich(b), rich(c))).join(''),
)}

<h2>7. 검 증 도 구</h2>
<p class="lead">
  아래 검사는 저장소에 포함되어 있으며, 인수 시점에 <strong>모두 통과한 상태</strong>로 인도한다.
  검사를 여럿 두는 것은 하나가 나머지를 대신하지 못하기 때문이다.
</p>
${table(
  th('검사', 160) + th('확인하는 것') + th('적용 단계', 90),
  CHECKS.map(([a, b, c]) => cells(rich(a), rich(b), esc(c))).join(''),
)}
`;

const out = 'FnB-비기능명세서.html';
writeFileSync(out, shell({ title: '비기능 명세서', kind: '비기능 요건 · 4/4', body }), 'utf8');
console.log(`${out} — 공통 요건 ${NFR.length} · 화면별 ${screenTotal} · 데이터 정책 ${DATA_POLICY.length}`);
