# Churned Tenants

> Internal Admin · `/tenants/churned` · id `tenants-churned`
> 원본: `apps/internal-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Churned Tenants |
| 메뉴 위치 | 고객사 > 이탈 |
| URL | `/tenants/churned` |
| 화면 목적 | 떠난 고객사와 그 이유를 남긴다. |
| 기대 효과 | 왜 떠났는지가 다음 계약의 조건을 정한다. |
| 고객사 배포 연동 | 없음 — 이미 배포가 내려간 고객사다. |
| Figma 페이지 순번 | 14 |

여기서 정한 값이 반영되는 고객사의 배포다. 틀리면 고객사 사이트가 통째로 멈추고, 원인을 찾는 것도 우리 몫이 된다.

## 2. 기능 뷰

![Churned Tenants 데스크톱](/page-view/tenants-churned/tenants-churned-desktop.jpg)

세 너비(데스크톱·태블릿·모바일)와 예외 화면은 `apps/internal-admin/docs/page-view/tenants-churned/` 에서 본다.

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 툴바 윗줄 | 재계약 가능 여부 탭(건수 포함). 등록 단추는 두지 않는다 | — |
| 툴바 아랫줄 | 검색 · 이탈 사유 필터 | — |
| 목록 | 계약 기간 · 사유 · 누적 매출 · 재계약 가능 여부 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 고객사명·담당자·코드로 검색 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 이탈 사유로 거르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 고객사명·담당자·코드로 검색 → 결과가 화면에 바로 반영된다.
- 이탈 사유로 거르기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 고객사명·담당자·코드로 검색 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 이탈 사유로 거르기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 조건에 맞는 것이 없으면 목록 자리에 왜 비었는지 적는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 등록 단추를 두지 않는다 — 이탈은 만드는 것이 아니라 계약이 끝나서 생기는 결과다.
- 떠난 고객사를 지우지 않는다. 남는 것은 금액이 아니라 이유다.
- 재계약 가능 여부를 색만이 아니라 `가능`·`어려움` 글자로도 적는다.
- 줄을 누르면 읽기 창이 열린다. 이탈은 계약이 끝나서 생긴 결과라 고칠 것이 없고, 왜 떠났는지는 한 줄에 담기지 않는다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 이탈 사유별 추이
- 재계약 시도 기록

## 관련 문서

- [화면 캡처](/docs/page-view/tenants-churned) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
