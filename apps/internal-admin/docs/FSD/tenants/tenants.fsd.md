# Tenants

> Internal Admin · `/tenants` · id `tenants`
> 원본: `apps/internal-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Tenants |
| 메뉴 위치 | 고객사 > 고객 |
| URL | `/tenants` |
| 화면 목적 | 지금 맡고 있는 고객사를 찾고 상태를 본다. |
| 기대 효과 | 유지보수가 끝나가는 곳을 계약이 끊기기 전에 알아챈다. |
| 고객사 배포 연동 | 고객사의 배포 전체 — 여기 등록한 고객사에만 배포가 붙는다. |
| Figma 페이지 순번 | 10 |

여기서 정한 값이 반영되는 고객사의 배포다. 틀리면 고객사 사이트가 통째로 멈추고, 원인을 찾는 것도 우리 몫이 된다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 툴바 | 왼쪽 검색·플랜 필터, 오른쪽 끝 고객사 등록 | — |
| 목록 | 고객사 · 배포 · 플랜 · 유지보수 상태 | — |
| 등록 모달 | 계약 시점에 정해지는 값만 | 등록을 눌렀을 때 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 고객사명·담당자·도메인으로 검색 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 플랜으로 거르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 행을 눌러 상세로 이동 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 연동 단추로 그 고객사의 OAuth 설정으로 이동 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 고객사 등록 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 고객사명 | 계약서에 적히는 이름 | 텍스트 | 필수 | — | 무드하우스 |
| 담당자 | 고객사 쪽 담당자 | 텍스트 | 필수 | — | 김서연 |
| 플랜 | 구독 등급 | 선택 | 필수 | 기준 값의 플랜 이름 목록 | 스탠다드 |
| 유지보수 종료일 | 계약이 끝나는 날 | 텍스트 | 선택 | `YYYY-MM-DD`. 비우면 기한 없음 | 2027-03-01 |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 고객사 등록 | 등록 모달을 연다 | 목록에 새 줄이 생기고 토스트로 알린다 | 무엇이 비었는지 토스트로 알리고 모달을 닫지 않는다 |
| 상세 | 그 고객사의 상세로 간다 | — | — |
| 연동 | 그 고객사의 OAuth 설정으로 간다 | 고객사가 걸린 채로 열린다 | — |

## 5. 사용자 시나리오

### 정상

- 고객사명·담당자·도메인으로 검색 → 결과가 화면에 바로 반영된다.
- 플랜으로 거르기 → 결과가 화면에 바로 반영된다.
- 행을 눌러 상세로 이동 → 결과가 화면에 바로 반영된다.
- 연동 단추로 그 고객사의 OAuth 설정으로 이동 → 결과가 화면에 바로 반영된다.
- 고객사 등록 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 고객사명·담당자·도메인으로 검색 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 플랜으로 거르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 행을 눌러 상세로 이동 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 연동 단추로 그 고객사의 OAuth 설정으로 이동 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 고객사 등록 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 고객사명과 담당자가 비면 등록하지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 유지보수 종료일이 날짜 모양이 아니면 등록하지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 고객사명·담당자는 공백만으로 둘 수 없다.
- 유지보수 종료일은 `YYYY-MM-DD` 모양이다.

### 화면 정책

- 만료·만료 임박인 고객사가 목록 맨 위로 온다 — 끝난 뒤에 알면 되돌릴 수 없다.
- 배포가 없는 것은 오류가 아니라 아직 붙이지 않은 것이다. 빈 칸으로 두지 않고 그렇게 적는다.
- 등록은 목록 위 모달에서 끝난다 — 계약 시점에 적는 값이 넷뿐이다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 담당자별 보기
- 계약 문서 첨부

## 관련 문서

- [화면 캡처](/docs/page-view/tenants) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
