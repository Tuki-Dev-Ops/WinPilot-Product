# Billing Due

> Internal Admin · `/billing/due` · id `billing-due`
> 원본: `apps/internal-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Billing Due |
| 메뉴 위치 | 결제 > 예정일 |
| URL | `/billing/due` |
| 화면 목적 | 앞으로 받을 청구를 만들고 기한을 지킨다. |
| 기대 효과 | 기한이 지나 연체로 넘어가기 전에 손을 쓴다. |
| 고객사 배포 연동 | 없음 — 고객사에게는 회계가 따로 안내한다. |
| Figma 페이지 순번 | 60 |

여기서 정한 값이 반영되는 고객사의 배포다. 틀리면 고객사 사이트가 통째로 멈추고, 원인을 찾는 것도 우리 몫이 된다.

## 2. 기능 뷰

![Billing Due 데스크톱](/page-view/billing-due/billing-due-desktop.jpg)

세 너비(데스크톱·태블릿·모바일)와 예외 화면은 `apps/internal-admin/docs/page-view/billing-due/` 에서 본다.

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 툴바 윗줄 | 7일 안 · 견적 · 청구 탭(건수 포함) · 청구 등록 | — |
| 툴바 아랫줄 | 검색 · 고객사 · 청구 항목 필터 | — |
| 고객사 | 고객사 드롭다운 | — |
| 목록 | 고객사 · 항목 · 금액 · 기한 · 상태 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 항목·번호·고객사로 검색 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 항목으로 거르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 고객사로 거르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 청구 등록 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 고객사 | 누구에게 받는지 | 선택 | 필수 | — | 무드하우스 |
| 항목 | 무엇에 대한 청구인지 | 선택 | 필수 | 기준 값의 청구 항목 목록 | 유지보수 |
| 청구할 등급 | 금액 · 제목 · 기한이 이 등급에서 따라온다 | 선택 | 선택 | 유지보수일 때만 나타나고, 팔지 않는 등급은 고를 수 없다 | 엔터프라이즈 (월 1,200,000원) |
| 제목 | 안내에 그대로 적힌다 | 텍스트 | 필수 | — | 2026년 10월 유지보수 |
| 금액 | 원 단위 | 텍스트 | 필수 | 숫자만 | 1200000 |
| 기한 | 언제까지 받는지 | 텍스트 | 필수 | `YYYY-MM-DD`. 오늘 이후 | 2026-10-31 |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 청구 등록 | 등록 모달을 연다 | 견적 상태로 목록에 생긴다 | 무엇이 틀렸는지 토스트로 알리고 모달을 닫지 않는다 |

## 5. 사용자 시나리오

### 정상

- 항목·번호·고객사로 검색 → 결과가 화면에 바로 반영된다.
- 항목으로 거르기 → 결과가 화면에 바로 반영된다.
- 고객사로 거르기 → 결과가 화면에 바로 반영된다.
- 청구 등록 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 항목·번호·고객사로 검색 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 항목으로 거르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 고객사로 거르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 청구 등록 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 제목이나 금액이 비면 등록하지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 기한이 날짜 모양이 아니면 등록하지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 지난 기한으로는 등록하지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 제목·금액은 비울 수 없다.
- 기한은 `YYYY-MM-DD` 이며 오늘보다 앞설 수 없다.

### 화면 정책

- 견적도 함께 보여 준다 — 곧 확정될 돈이라 빠지면 다른 화면을 함께 열게 된다.
- 새로 만든 것은 언제나 견적이다. 확정은 회계가 세금계산서를 낼 때 일어난다.
- 청구를 집행하지 않고 안내만 한다 — 그래서 결제하기 단추가 없다.
- 유지보수는 등급 값을 그대로 청구한다. 고객사는 등급을 이름으로 들고 요금은 등급이 들고 있어, 사람이 눈으로 이어 붙이면 등급 값을 올린 달에 한 곳만 옛 금액으로 남는다.
- 등급을 고르면 금액 · 제목 · 기한이 함께 채워진다. 기한이 월말인 것은 유지보수가 달 단위이기 때문이고, 30일과 31일을 손으로 적으면 2월마다 틀린다.
- 손으로 고친 칸은 등급을 바꿔도 덮지 않는다 — 이번 달만 깎아 주기로 한 협의가 지워진 줄도 모르고 저장된다.
- 이미 있는 청구를 열 때는 적힌 값이 옳다. 등급에서 다시 끌어오면 깎아 준 금액이 열자마자 원래대로 돌아간다.
- 등급 칸은 유지보수일 때만 뜬다. 구축·추가 개발·호스팅은 등급과 무관한 금액이라 고른 값이 아무 데도 쓰이지 않는다.
- 팔지 않는 등급은 고를 수 없다 — 계약할 수 없는 값이 청구서에 적힌다.
- 줄을 누르면 그 건의 값이 담긴 같은 창이 열린다. 상태와 발행일은 그 창에서 바꾸지 않는다 — 회계가 세금계산서를 낼 때 움직이는 값이다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 반복 청구 자동 생성
- 세금계산서 상태 연동

## 관련 문서

- [화면 캡처](/docs/page-view/billing-due) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
