# Staff

> Internal Admin · `/settings/staff` · id `settings-staff`
> 원본: `apps/internal-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Staff |
| 메뉴 위치 | 설정 > 사내 계정 |
| URL | `/settings/staff` |
| 화면 목적 | 이 콘솔에 들어오는 우리 직원을 관리한다. |
| 기대 효과 | 되돌릴 수 없는 일을 누가 할 수 있는지가 한자리에서 보인다. |
| 고객사 배포 연동 | 없음 — 고객사가 아니라 우리 직원의 계정이다. |
| Figma 페이지 순번 | 70 |

여기서 정한 값이 반영되는 고객사의 배포다. 틀리면 고객사 사이트가 통째로 멈추고, 원인을 찾는 것도 우리 몫이 된다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 요약 카드 | 쓰는 계정 · 관리자 · 중지된 계정 | — |
| 직급이 여는 것 | 직급마다 무엇을 주는지 | — |
| 툴바 | 왼쪽 검색·직급 필터, 오른쪽 끝 사내 계정 등록 | — |
| 목록 | 이름 · 계정 · 맡은 고객사 · 마지막 접속 · 직급 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 이름·계정·소속으로 검색 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 직급으로 거르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 사내 계정 등록 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 이름 | 직원 이름 | 텍스트 | 필수 | — | 박현우 |
| 계정 | 사내 메일 주소 | 텍스트 | 필수 | `아이디@도메인` 모양 | name@winpilot.test |
| 소속 | 누구에게 물어야 하는지 | 텍스트 | 필수 | — | 고객성공 |
| 직급 | 이 콘솔에서 무엇을 할 수 있는지 | 선택 | 필수 | 새 계정은 조회로 시작한다 | 조회 |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 사내 계정 등록 | 등록 모달을 연다 | 조회 직급으로 목록에 생긴다 | 무엇이 틀렸는지 토스트로 알리고 모달을 닫지 않는다 |

## 5. 사용자 시나리오

### 정상

- 이름·계정·소속으로 검색 → 결과가 화면에 바로 반영된다.
- 직급으로 거르기 → 결과가 화면에 바로 반영된다.
- 사내 계정 등록 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 이름·계정·소속으로 검색 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 직급으로 거르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 사내 계정 등록 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 이름이나 소속이 비면 등록하지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 이메일 모양이 아니면 등록하지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 이미 있는 계정이면 등록하지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 이름·소속은 공백만으로 둘 수 없다.
- 이메일 모양이 아니면 막는다.
- 같은 계정을 두 번 등록하지 않는다.

### 화면 정책

- 목록 위에 직급이 무엇을 여는지 먼저 적는다 — 이름만 보고 고르면 무엇을 주는지 모른 채 준다.
- 새 계정은 언제나 조회로 시작한다. 처음부터 되돌릴 수 없는 일까지 열어 주지 않는다.
- 중지한 계정을 지우지 않는다 — 지난 기록에 이름이 남아 있다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 로그인 이력
- 2단계 인증

## 관련 문서

- [화면 캡처](/docs/page-view/settings-staff) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
