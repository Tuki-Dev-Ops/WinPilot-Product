# Revenue

> Internal Admin · `/statistics/revenue` · id `statistics-revenue`
> 원본: `apps/internal-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Revenue |
| 메뉴 위치 | 통계 > 매출 |
| URL | `/statistics/revenue` |
| 화면 목적 | 우리가 고객사에게 받는 돈의 흐름을 읽는다. |
| 기대 효과 | 한 곳에 매출이 쏠려 있는지를 계약이 끝나기 전에 안다. |
| 고객사 배포 연동 | 없음 — 운영자만 보는 화면이다. |
| Figma 페이지 순번 | 50 |

여기서 정한 값이 반영되는 고객사의 배포다. 틀리면 고객사 사이트가 통째로 멈추고, 원인을 찾는 것도 우리 몫이 된다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 요약 카드 | 최근 12개월 · 이번 달 · 앞 달 대비 · 월평균 | — |
| 달별 매출 | 인라인 SVG 막대 | — |
| 무엇으로 · 어디서 | 항목별·고객사별 비중 | — |
| 달별 내역 | 차트와 같은 값의 표 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 달별 막대 훑기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 무엇으로 버는지 보기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 어디서 버는지 보기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 표에서 숫자 옮겨 적기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 달별 막대 훑기 → 결과가 화면에 바로 반영된다.
- 무엇으로 버는지 보기 → 결과가 화면에 바로 반영된다.
- 어디서 버는지 보기 → 결과가 화면에 바로 반영된다.
- 표에서 숫자 옮겨 적기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 달별 막대 훑기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 무엇으로 버는지 보기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 어디서 버는지 보기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 표에서 숫자 옮겨 적기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 읽기만 하는 화면이라 막을 조건이 없다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 막대의 바닥은 언제나 0 이다 — 최솟값을 바닥으로 잡으면 없는 추세를 읽는다.
- 차트는 인라인 SVG 다. 비트맵은 Figma 에서 벡터로 복원되지 않는다.
- 증감은 색만이 아니라 부호(+·-)로도 적는다.
- 차트와 표가 같은 배열을 읽는다 — 두 곳의 값이 갈리지 않게.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 기간 직접 고르기
- 해마다 비교
- 내려받기

## 관련 문서

- [화면 캡처](/docs/page-view/statistics-revenue) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
