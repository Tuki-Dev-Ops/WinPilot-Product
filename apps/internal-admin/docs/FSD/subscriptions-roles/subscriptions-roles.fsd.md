# Roles

> Internal Admin · `/subscriptions/roles` · id `subscriptions-roles`
> 원본: `apps/internal-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Roles |
| 메뉴 위치 | 구독 > 권한 |
| URL | `/subscriptions/roles` |
| 화면 목적 | 고객사가 자기 콘솔에서 쓰는 권한을 정한다. |
| 기대 효과 | "이 기능 쓰려면 얼마부터인가" 를 매번 말로 설명하지 않는다. |
| 고객사 배포 연동 | 고객사 배포의 어드민 메뉴 — 꺼진 권한의 메뉴는 그 배포에 아예 없다. |
| Figma 페이지 순번 | 21 |

여기서 정한 값이 반영되는 고객사의 배포다. 틀리면 고객사 사이트가 통째로 멈추고, 원인을 찾는 것도 우리 몫이 된다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 요약 카드 | 권한 수 · 끌 수 없는 권한 · 플랜 수 | — |
| 툴바 | 왼쪽 검색·끌 수 있는지 필터, 오른쪽 끝 권한 등록 | — |
| 목록 | 권한 · 여는 것 · 열리는 플랜 · 끌 수 있는지 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 권한 이름·여는 것으로 검색 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 끌 수 있는지로 거르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 권한 등록 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 권한 이름 | 화면에 보이는 말 | 텍스트 | 필수 | — | 배송비 설정 |
| 여는 것 | 고객사가 무엇을 할 수 있게 되는지 한 문장 | 텍스트 | 필수 | — | 배송비 규칙을 만들고 고친다. |
| 처음 열리는 플랜 | 이 플랜부터 위로 모두 켜진다 | 선택 | 필수 | — | 스탠다드 |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 권한 등록 | 등록 모달을 연다 | 끌 수 있는 권한으로 목록에 생긴다 | 무엇이 비었는지 토스트로 알리고 모달을 닫지 않는다 |

## 5. 사용자 시나리오

### 정상

- 권한 이름·여는 것으로 검색 → 결과가 화면에 바로 반영된다.
- 끌 수 있는지로 거르기 → 결과가 화면에 바로 반영된다.
- 권한 등록 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 권한 이름·여는 것으로 검색 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 끌 수 있는지로 거르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 권한 등록 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 권한 이름이나 여는 것이 비면 등록하지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 권한 이름과 여는 것은 공백만으로 둘 수 없다.

### 화면 정책

- 여기 권한은 고객사가 쓰는 것이다 — 이 콘솔에 들어오는 직원은 설정 · 사내 계정에 있다.
- 새 권한은 끌 수 있는 것으로 만든다. 끌 수 없는 권한은 제품이 도는 데 필요한 것이라 화면에서 만들지 않는다.
- 진하게 칠한 플랜이 그 권한이 처음 열리는 플랜이다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 권한별 사용량
- 고객사별 권한 덮어쓰기

## 관련 문서

- [화면 캡처](/docs/page-view/subscriptions-roles) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
