# Signup

> B2C Client 템플릿 A · `/signup` · id `signup`
> 원본: `apps/b2c-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Signup |
| 메뉴 위치 | 계정 > 회원가입 |
| URL | `/signup` |
| 화면 목적 | 계정을 만든다. |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 어드민 연동 | 사용자 > 사용자 추가(목록 안 모달)와 같은 항목 |
| Figma 페이지 순번 | 41 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

![Signup 데스크톱](/page-view/signup/signup-desktop.jpg)

세 너비(데스크톱·태블릿·모바일)와 예외 화면은 [화면 캡처](/docs/page-view/signup)에서 본다.

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 탭 | 로그인 · 회원가입 | — |
| 입력 묶음 | 이메일·비밀번호·닉네임 | — |
| 약관 동의 | 필수·선택을 갈라 보여 준다 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 항목 입력 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 닉네임 자동 생성 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 이메일 인증(발송·확인) | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 가입(확인 창) | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 이메일 | 계정 | 텍스트 | 필수 | `아이디@도메인` 모양 | hello@example.com |
| 비밀번호 | 계정 비밀번호 | 비밀번호 | 필수 | 8자 이상 · 영문+숫자 | — |
| 비밀번호 확인 | 같은 값 다시 | 비밀번호 | 필수 | 위와 같아야 함 | — |
| 닉네임 | 화면에 보이는 이름 | 텍스트 | 필수 | 2~12자 · 금칙어 불가 | 바람가르는하늘 |
| 필수 약관 동의 | 이용약관·개인정보 | 체크박스 | 필수 | — | — |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 가입하기 | 입력을 확인하고 계정을 만든다 | `/result?state=done&kind=signup` 으로 이동 | 틀린 입력으로 포커스를 옮긴다 |

## 5. 사용자 시나리오

### 정상

- 항목 입력 → 결과가 화면에 바로 반영된다.
- 닉네임 자동 생성 → 결과가 화면에 바로 반영된다.
- 이메일 인증(발송·확인) → 결과가 화면에 바로 반영된다.
- 가입(확인 창) → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 항목 입력 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 닉네임 자동 생성 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 이메일 인증(발송·확인) **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 가입(확인 창) **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 이메일 인증을 마쳐야 가입할 수 있다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 인증 뒤 주소를 고치면 인증이 풀린다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 비밀번호 8자 이상·확인 일치·필수 동의. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 비밀번호와 확인 값이 다르면 그 자리에서 알린다.
- 닉네임은 2~12자이며 금칙어를 쓸 수 없다.
- 필수 약관에 동의하지 않으면 단추가 잠긴다.

### 화면 정책

- 약관 본문은 링크로 열리며 로그인 없이 읽힌다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 이메일 인증. 지금은 형식만 본다.

## 관련 문서

- [화면 캡처](/docs/page-view/signup) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
