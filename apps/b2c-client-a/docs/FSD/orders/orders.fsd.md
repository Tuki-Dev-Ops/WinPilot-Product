# Orders

> B2C Client 템플릿 A · `/orders` · id `orders`
> 원본: `apps/b2c-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Orders |
| 메뉴 위치 | 마이페이지 > 주문 내역 |
| URL | `/orders` |
| 화면 목적 | 내 주문이 지금 어디까지 왔는지 본다. |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 어드민 연동 | 판매 목록 (`/products/sales`) — 고객 화면의 '주문' 과 같은 자원 |
| Figma 페이지 순번 | 32 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

![Orders 데스크톱](/page-view/orders/orders-desktop.jpg)

세 너비(데스크톱·태블릿·모바일)와 예외 화면은 `apps/b2c-client-a/docs/page-view/orders/` 에서 본다.

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 기간 필터 | 언제 것을 볼지 | — |
| 주문 목록 | 주문번호·상품·금액·상태 | — |
| 빈 상태 | 주문이 없을 때 | 목록 0건 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 배송 상태 탭으로 좁히기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 주문 상세 열기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 기간 | 조회 범위 | 선택 | 선택 | 최근 1개월·3개월·6개월 | 최근 3개월 |

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 배송 상태 탭으로 좁히기 → 결과가 화면에 바로 반영된다.
- 주문 상세 열기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 배송 상태 탭으로 좁히기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 주문 상세 열기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 한 건도 없는 상태는 탭에서 뺀다 — 늘 0 인 탭이 줄지어 있으면 탭 줄만 길어진다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 기본 정렬은 주문일 최신순이다.
- 기간과 쪽을 주소에 둔다.
- 주문은 마이페이지에 딸린 것이 아니라 독립된 자원이라 주소가 `/orders` 다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 주문 상태별 필터(배송중·완료).

## 관련 문서

- [화면 캡처](/docs/page-view/orders) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
