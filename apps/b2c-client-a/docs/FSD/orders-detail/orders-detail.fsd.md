# Order Detail

> B2C Client 템플릿 A · `/orders/[orderId]` · id `orders-detail`
> 원본: `apps/b2c-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Order Detail |
| 메뉴 위치 | 마이페이지 > 주문 상세 |
| URL | `/orders/[orderId]` · 예 `/orders/S-24081` |
| 화면 목적 | 한 주문의 결제·배송·운송장을 확인한다. |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 어드민 연동 | 판매 상세 — 운송장·결제 취소·교환은 운영자가 처리한다 |
| Figma 페이지 순번 | 34 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

![Order Detail 데스크톱](/page-view/orders-detail/orders-detail-desktop.jpg)

세 너비(데스크톱·태블릿·모바일)와 예외 화면은 [화면 캡처](/docs/page-view/orders-detail)에서 본다.

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 주문 요약 | 주문번호·주문일·상태 | — |
| 상품 목록 | 산 것과 금액 | — |
| 배송 정보 | 받는 사람·주소·운송장 | 배송이 시작된 뒤 |
| 결제 정보 | 금액 내역 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 운송장 번호 확인 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 주문 목록으로 돌아가기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 운송장 번호 확인 → 결과가 화면에 바로 반영된다.
- 주문 목록으로 돌아가기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 운송장 번호 확인 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 주문 목록으로 돌아가기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 없는 주문번호는 404 다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 내 주문이 아니면 열리지 않는다.
- 취소·교환은 상태에 따라 보이는 것이 달라진다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 취소·교환·반품 신청. 지금은 상태만 보여 준다.

## 관련 문서

- [화면 캡처](/docs/page-view/orders-detail) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
