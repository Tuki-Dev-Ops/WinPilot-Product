# 기능 명세 — My Page

> B2C Client 템플릿 A · 화면 id `mypage`
> 원본: `apps/b2c-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 문서 정보

| 항목 | 내용 |
|---|---|
| 문서명 | 기능 명세서 — My Page |
| 화면명 | My Page |
| 메뉴 위치 | 마이페이지 > 내 정보 수정 |
| URL | `/mypage` |
| 화면 설명 | 내 정보를 확인하고 필요할 때만 고친다. |
| Figma 페이지 순번 | 35 |
| 개정 이력 | 원본(`screen-specs.ts`)의 변경 이력을 따른다 — 문서를 따로 관리하지 않는다 |

## 2. 목적과 배경

**화면 목적** — 내 정보를 확인하고 필요할 때만 고친다.

**기대 효과** — 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다.

**어드민 연동** — 이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

- 사용자 > 사용자 목록의 상세
- 사용자 > 등급

## 3. 화면 구성

_영역 구분이 따로 없는 한 덩어리 화면입니다._

## 4. 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 5. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 수정 시작·취소 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 닉네임 자동 생성 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 주소 찾기(우편번호 서비스) | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 마케팅 동의 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 저장(확인 창) | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

## 6. 버튼과 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 7. 사용자 시나리오

**정상**

- 수정 시작·취소 → 결과가 화면에 바로 반영된다.
- 닉네임 자동 생성 → 결과가 화면에 바로 반영된다.
- 주소 찾기(우편번호 서비스) → 결과가 화면에 바로 반영된다.
- 마케팅 동의 → 결과가 화면에 바로 반영된다.
- 저장(확인 창) → 결과가 화면에 바로 반영된다.

**예외**

- 이메일·이름은 고칠 수 없다 — 주문·문의 기록을 묶는 값이고 실명이다.
- 연락처는 숫자 9~11자리만 통과한다.
- 주소 서비스가 막혀도 직접 입력할 수 있다.

## 8. 예외 처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 이메일·이름은 고칠 수 없다 — 주문·문의 기록을 묶는 값이고 실명이다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 연락처는 숫자 9~11자리만 통과한다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 주소 서비스가 막혀도 직접 입력할 수 있다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 9. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

## 10. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

## 11. UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

## 12. 화면 정책

_N/A — 이 화면에는 해당되지 않습니다._

## 13. 인수 조건

- **Given** 이 화면을 열었을 때 **When** 수정 시작·취소 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 닉네임 자동 생성 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 주소 찾기(우편번호 서비스) **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 마케팅 동의 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 저장(확인 창) **Then** 그 결과가 화면에 반영된다.

## 14. 향후 확장

_N/A — 이 화면에는 해당되지 않습니다._

## 관련 문서

- [비기능 명세](/docs/nfs)
- [화면 캡처](/docs/page-view/mypage)
- [IA](/docs/ia) · [Flow Chart](/docs/flow-chart)
