# Contact

> B2C Client 템플릿 A · `/contact` · id `contact`
> 원본: `apps/b2c-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Contact |
| 메뉴 위치 | 고객지원 > 문의하기 |
| URL | `/contact` |
| 화면 목적 | 문의를 보낸다. |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 어드민 연동 | 문의 > 설정 · 문의 > 목록(Path `/contact`) |
| Figma 페이지 순번 | 51 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

![Contact 데스크톱](/page-view/contact/contact-desktop.jpg)

세 너비(데스크톱·태블릿·모바일)와 예외 화면은 `apps/b2c-client-a/docs/page-view/contact/` 에서 본다.

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 왼쪽 aside | 고객지원 갈래 | — |
| 안내문 | 무엇을 적어야 하는지 | — |
| 문의 양식 | 분류·제목·내용·연락처 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 유형 선택 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 항목 입력 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 파일 첨부 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 개인정보 동의 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 보내기(확인 창) | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 문의 분류 | 무엇에 대한 문의인지 | 선택 | 필수 | 어드민이 정한 분류 | 배송 |
| 제목 | 한 줄 요약 | 텍스트 | 필수 | 2~50자 | 배송이 늦습니다 |
| 내용 | 자세한 내용 | 여러 줄 | 필수 | 10자 이상 | — |
| 연락처 | 답변 받을 곳 | 텍스트 | 필수 | 이메일 또는 전화 | hello@example.com |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 보내기 | 입력을 확인하고 접수한다 | `/result?state=done&kind=inquiry` 로 이동 | 틀린 입력으로 포커스를 옮긴다 |

## 5. 사용자 시나리오

### 정상

- 유형 선택 → 결과가 화면에 바로 반영된다.
- 항목 입력 → 결과가 화면에 바로 반영된다.
- 파일 첨부 → 결과가 화면에 바로 반영된다.
- 개인정보 동의 → 결과가 화면에 바로 반영된다.
- 보내기(확인 창) → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 유형 선택 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 항목 입력 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 파일 첨부 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 개인정보 동의 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 보내기(확인 창) **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 어드민이 켠 항목만 나오고, 필수 여부도 어드민을 따른다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 첨부는 고른 순간 형식·용량·개수를 검사한다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 필수 동의 없이는 보낼 수 없다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 제목은 2~50자, 내용은 10자 이상이다.
- 연락처는 이메일 또는 전화번호 모양이어야 한다.
- 필수 항목이 비면 단추가 잠긴다.

### 화면 정책

- 문의 분류는 어드민이 정한다 — 템플릿에 박아 두지 않는다.
- 보낸 문의는 마이페이지의 문의 내역에서 다시 본다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 파일 첨부. 지금은 글만 받는다.

## 관련 문서

- [화면 캡처](/docs/page-view/contact) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
