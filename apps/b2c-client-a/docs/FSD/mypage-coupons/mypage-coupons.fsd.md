# My Page Coupons

> B2C Client 템플릿 A · `/mypage/coupons` · id `mypage-coupons`
> 원본: `apps/b2c-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | My Page Coupons |
| 메뉴 위치 | 마이페이지 > 쿠폰함 |
| URL | `/mypage/coupons` |
| 화면 목적 | 보유 쿠폰과 발급 가능한 쿠폰을 조회하는 쿠폰함 페이지입니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 어드민 연동 | store: COUPONS |
| Figma 페이지 순번 | 37 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

![My Page Coupons 데스크톱](/page-view/mypage-coupons/mypage-coupons-desktop.jpg)

세 너비(데스크톱·태블릿·모바일)와 예외 화면은 `apps/b2c-client-a/docs/page-view/mypage-coupons/` 에서 본다.

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 마이페이지 메뉴 | 마이페이지 하위 메뉴 | — |
| 상태 탭 | 사용 가능 · 사용 완료 · 기간 만료 | — |
| 쿠폰 목록 | 쿠폰명 · 할인 내용 · 사용 기한 | — |
| 발급 가능 쿠폰 목록 | 발급 대상 쿠폰 | 발급 가능한 쿠폰이 있을 때 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 처리 | 결과 |
|---|---|---|---|
| 보유 쿠폰 / 발급 가능 쿠폰 탭 전환 기능 제공 | 보유 쿠폰 / 발급 가능 쿠폰 탭 전환 | 정의 필요 | 정의 필요 |
| 쿠폰 발급 기능 제공 | 쿠폰 발급 | 선택한 쿠폰을 쿠폰함에 추가한다 | 목록에 추가하고 발급 완료를 안내한다 / 이미 발급받은 쿠폰이면 사유를 표시한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 쿠폰 발급 | 선택한 쿠폰을 쿠폰함에 추가한다 | 목록에 추가하고 발급 완료를 안내한다 | 이미 발급받은 쿠폰이면 사유를 표시한다 |

## 5. 사용자 시나리오

### 정상

- 보유 쿠폰 / 발급 가능 쿠폰 탭 전환 → 결과가 화면에 바로 반영된다.
- 쿠폰 발급 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 보유 쿠폰 / 발급 가능 쿠폰 탭 전환 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 쿠폰 발급 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 사용 기간이 종료된 쿠폰도 숨기지 않고 비활성 상태로 표시합니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 기본 탭은 사용 가능입니다.
- 만료된 쿠폰도 목록에서 제거하지 않습니다. 사용할 수 없는 사유가 확인되어야 합니다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 쿠폰 만료 알림.

## 관련 문서

- [화면 캡처](/docs/page-view/mypage-coupons) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
