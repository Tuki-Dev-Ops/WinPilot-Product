# 기능 명세 — Settings Privacy

> B2C Admin · 화면 id `settings-privacy`
> 원본: `apps/b2c-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 문서 정보

| 항목 | 내용 |
|---|---|
| 문서명 | 기능 명세서 — Settings Privacy |
| 화면명 | Settings Privacy |
| 메뉴 위치 | 설정 > 개인정보 처리방침 정보 |
| URL | `/settings/privacy` |
| 화면 설명 | 개인정보 처리방침를 정한다. |
| Figma 페이지 순번 | 73 |
| 개정 이력 | 원본(`screen-specs.ts`)의 변경 이력을 따른다 — 문서를 따로 관리하지 않는다 |

## 2. 목적과 배경

**화면 목적** — 개인정보 처리방침를 정한다.

**기대 효과** — 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다.

**고객 화면 연동** — 여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

- 개인정보 처리방침

## 3. 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 입력 묶음 | 고칠 값들 | — |
| 미리보기 | 고객 화면에서 어떻게 보이는지 | 값을 넣었을 때 |
| 저장 줄 | 저장·취소 | — |

## 4. 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 본문 | 고객이 읽을 처리방침 | 편집기 | 필수 | — | — |
| 시행일 | 이 판이 적용되는 날 | 날짜 | 필수 | `YYYY-MM-DD` | 2026-08-01 |

## 5. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 값 수정 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 저장 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

## 6. 버튼과 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 저장 | 입력을 확인하고 반영한다 | 토스트로 알리고 목록으로 돌아간다 | 틀린 입력으로 포커스를 옮긴다 |
| 취소 | 고친 것을 버린다 | 목록으로 돌아간다 | 고친 것이 있으면 확인을 한 번 묻는다 |

## 7. 사용자 시나리오

**정상**

- 값 수정 → 결과가 화면에 바로 반영된다.
- 저장 → 결과가 화면에 바로 반영된다.

**예외**

- 필수 항목이 비면 저장 단추가 잠긴다.

## 8. 예외 처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 필수 항목이 비면 저장 단추가 잠긴다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 9. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

## 10. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

## 11. UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

## 12. 화면 정책

- 값을 템플릿에 박아 두지 않는다. 박아 두면 고칠 때마다 배포해야 한다.

## 13. 인수 조건

- **Given** 이 화면을 열었을 때 **When** 값 수정 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 저장 **Then** 그 결과가 화면에 반영된다.

## 14. 향후 확장

- 변경 이력.

## 관련 문서

- [비기능 명세](/docs/nfs)
- [화면 캡처](/docs/page-view/settings-privacy)
- [IA](/docs/ia) · [Flow Chart](/docs/flow-chart)
