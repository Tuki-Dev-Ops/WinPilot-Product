# 기능 명세 — Users

> B2C Admin · 화면 id `users`
> 원본: `apps/b2c-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 문서 정보

| 항목 | 내용 |
|---|---|
| 문서명 | 기능 명세서 — Users |
| 화면명 | Users |
| 메뉴 위치 | 사용자 > 사용자 |
| URL | `/users` |
| 화면 설명 | 가입한 사람을 찾고 상태를 본다. |
| Figma 페이지 순번 | 10 |
| 개정 이력 | 원본(`screen-specs.ts`)의 변경 이력을 따른다 — 문서를 따로 관리하지 않는다 |

## 2. 목적과 배경

**화면 목적** — 가입한 사람을 찾고 상태를 본다.

**기대 효과** — 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다.

**고객 화면 연동** — 여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

- 마이페이지 — 여기서 바꾼 등급이 고객 화면의 혜택에 반영된다.

## 3. 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 목록 툴바 | 검색·필터·등록 단추 | — |
| 표 | 한 줄이 자원 하나 | — |
| 선택 도구 | 고른 줄에 한 번에 하는 일 | 줄을 골랐을 때 |
| 쪽 넘김 | 몇 쪽 중 몇 쪽인지 | — |
| 빈 상태 | 조건에 맞는 것이 없을 때 | 결과 0건 |

## 4. 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 5. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 이름·닉네임·이메일·회원번호로 검색 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 상태 탭으로 거르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 조회·삭제 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

## 6. 버튼과 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 7. 사용자 시나리오

**정상**

- 이름·닉네임·이메일·회원번호로 검색 → 결과가 화면에 바로 반영된다.
- 상태 탭으로 거르기 → 결과가 화면에 바로 반영된다.
- 조회·삭제 → 결과가 화면에 바로 반영된다.

**예외**

- 삭제는 확인을 한 번 묻는다.
- 개인정보는 목록에서 가려서 보여 준다.

## 8. 예외 처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 삭제는 확인을 한 번 묻는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 개인정보는 목록에서 가려서 보여 준다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 9. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

## 10. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

## 11. UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

## 12. 화면 정책

- 기본 정렬은 등록 최신순이다.
- 검색·필터·쪽을 주소에 둔다 — 새로고침·뒤로가기에서 조건이 살아남는다.
- 검색어가 비면 조건이 없는 것으로 본다. 목록을 비우지 않는다.
- 기본 탭은 전체다.

## 13. 인수 조건

- **Given** 이 화면을 열었을 때 **When** 이름·닉네임·이메일·회원번호로 검색 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 상태 탭으로 거르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 조회·삭제 **Then** 그 결과가 화면에 반영된다.

## 14. 향후 확장

- 내보내기(CSV).

## 관련 문서

- [비기능 명세](/docs/nfs)
- [화면 캡처](/docs/page-view/users)
- [IA](/docs/ia) · [Flow Chart](/docs/flow-chart)
