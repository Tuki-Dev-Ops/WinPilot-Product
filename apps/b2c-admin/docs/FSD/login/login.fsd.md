# Login

> B2C Admin · `/login` · id `login`
> 원본: `apps/b2c-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Login |
| 메뉴 위치 | 메뉴 밖 |
| URL | `/login` |
| 화면 목적 | 운영자만 들어오게 한다. |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 없음 — 고객 화면과 계정이 다르다. |
| Figma 페이지 순번 | 2 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

![Login 데스크톱](/page-view/login/login-desktop.jpg)

세 너비(데스크톱·태블릿·모바일)와 예외 화면은 [화면 캡처](/docs/page-view/login)에서 본다.

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 입력 묶음 | 이메일·비밀번호 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 이메일·비밀번호로 로그인 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 이메일 | 운영자 계정 | 텍스트 | 필수 | `아이디@도메인` 모양 | demo@winpilot.test |
| 비밀번호 | 계정 비밀번호 | 비밀번호 | 필수 | 8자 이상 | — |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 로그인 | 입력을 확인하고 들어간다 | 대시보드로 이동 | 무엇이 틀렸는지 입력 아래 적는다 |

## 5. 사용자 시나리오

### 정상

- 이메일·비밀번호로 로그인 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 이메일·비밀번호로 로그인 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 둘 중 하나라도 비면 단추가 잠긴다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 이메일 모양이 아니면 그 자리에서 알린다.
- 비밀번호는 8자 이상이다.

### 화면 정책

_N/A — 이 화면에는 해당되지 않습니다._

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 2단계 인증.

## 관련 문서

- [화면 캡처](/docs/page-view/login) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
