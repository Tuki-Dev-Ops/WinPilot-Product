# Product Sale Detail

> B2C Admin · `/products/sales/[orderId]` · id `products-sales-detail`
> 원본: `apps/b2c-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Product Sale Detail |
| 메뉴 위치 | 상품 > (목록에서 진입) |
| URL | `/products/sales/[orderId]` · 예 `/products/sales/S-24081` |
| 화면 목적 | 주문 한 건을 자세히 본다. |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 주문 상세 |
| Figma 페이지 순번 | 25 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

![Product Sale Detail 데스크톱](/page-view/products-sales-detail/products-sales-detail-desktop.jpg)

세 너비(데스크톱·태블릿·모바일)와 예외 화면은 `apps/b2c-admin/docs/page-view/products-sales-detail/` 에서 본다.

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 주문 요약 | 주문번호·주문일·상태 | — |
| 상품 | 산 것과 금액 | — |
| 배송 | 받는 사람·주소·운송장 | — |
| 결제 | 금액 내역 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 주문 상품·금액 확인 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 배송 정보 수정 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 상태 바꾸기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 주문 상품·금액 확인 → 결과가 화면에 바로 반영된다.
- 배송 정보 수정 → 결과가 화면에 바로 반영된다.
- 상태 바꾸기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 주문 상품·금액 확인 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 배송 정보 수정 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 상태 바꾸기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 취소된 주문은 상태를 되돌릴 수 없다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 금액은 고칠 수 없다 — 결제된 값이라 화면에서 바꾸면 두 값이 갈린다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 부분 취소.

## 관련 문서

- [화면 캡처](/docs/page-view/products-sales-detail) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
