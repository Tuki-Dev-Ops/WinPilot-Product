# 기능 명세 — Components

> B2C Admin · 화면 id `components`
> 원본: `apps/b2c-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 문서 정보

| 항목 | 내용 |
|---|---|
| 문서명 | 기능 명세서 — Components |
| 화면명 | Components |
| 메뉴 위치 | 메뉴 밖 |
| URL | `/ssot/components` |
| 화면 설명 | 쓰고 있는 컴포넌트를 실제로 그려 본다. |
| Figma 페이지 순번 | 100 |
| 개정 이력 | 원본(`screen-specs.ts`)의 변경 이력을 따른다 — 문서를 따로 관리하지 않는다 |

## 2. 목적과 배경

**화면 목적** — 쓰고 있는 컴포넌트를 실제로 그려 본다.

**기대 효과** — 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다.

**고객 화면 연동** — 여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

- 없음 — 운영자에게 보이지 않는 개발 도구다.

## 3. 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 컴포넌트 목록 | 이름과 실제 모습 | — |

## 4. 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 5. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 컴포넌트 훑어보기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

## 6. 버튼과 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 7. 사용자 시나리오

**정상**

- 컴포넌트 훑어보기 → 결과가 화면에 바로 반영된다.

**예외**

- 없다 — 읽기만 하는 개발용 화면이다.

## 8. 예외 처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 없다 — 읽기만 하는 개발용 화면이다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 9. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

## 10. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

## 11. UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

## 12. 화면 정책

- 이 화면은 고객사에 노출되지 않는다.

## 13. 인수 조건

- **Given** 이 화면을 열었을 때 **When** 컴포넌트 훑어보기 **Then** 그 결과가 화면에 반영된다.

## 14. 향후 확장

- 어느 화면에 쓰이는지 표시.

## 관련 문서

- [비기능 명세](/docs/nfs)
- [화면 캡처](/docs/page-view/components)
- [IA](/docs/ia) · [Flow Chart](/docs/flow-chart)
