# Banner Create

> B2C Admin · `/banners/new` · id `banners-new`
> 원본: `apps/b2c-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Banner Create |
| 메뉴 위치 | 배너 > (목록에서 진입) |
| URL | `/banners/new` |
| 화면 목적 | 메인 배너를 등록하는 페이지입니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 메인 배너 영역 |
| Figma 페이지 순번 | 51 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

![Banner Create 데스크톱](/page-view/banners-new/banners-new-desktop.jpg)

세 너비(데스크톱·태블릿·모바일)와 예외 화면은 `apps/b2c-admin/docs/page-view/banners-new/` 에서 본다.

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 입력 영역 | 등록 및 수정 항목 | — |
| 미리보기 영역 | 고객 화면에 표시되는 형태 | 입력값이 있을 때 |
| 저장 버튼 영역 | 저장 · 취소 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 처리 | 결과 |
|---|---|---|---|
| 이미지 등록 기능 제공 | 이미지 등록 | 입력값을 검증한 뒤 신규 항목으로 저장한다 | 저장 결과를 안내하고 목록 화면으로 이동한다 |
| 연결 URL 등록 기능 제공 | 연결 URL 입력 | 입력값을 검증한 뒤 신규 항목으로 저장한다 | 저장 결과를 안내하고 목록 화면으로 이동한다 |
| 게시 기간 상태 변경 기능 제공 | 게시 기간 설정 | 해당 항목의 상태값을 변경한다 | 변경된 상태를 목록과 고객 화면에 반영한다 |
| 미리보기 조회 기능 제공 | 미리보기 조회 | 등록된 데이터를 조회한다 | 조회한 정보를 화면에 표시한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 이미지 | 배너 이미지 | 파일 | 필수 | PNG · JPG · WEBP · 5MB 이하 · 21:9 | — |
| 연결 URL | 배너 선택 시 이동 경로 | 텍스트 | 선택 | 사이트 내 경로 | /products?tag=NEW |
| 게시 기간 | 노출 기간 | 기간 | 필수 | 시작일 ≤ 종료일 | 2026-08-01 ~ 2026-08-31 |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 저장 | 입력값을 검증한 뒤 저장한다 | 저장 완료를 안내하고 목록 화면으로 이동한다 | 검증에 실패한 입력 항목으로 포커스를 이동한다 |
| 취소 | 수정 내용을 반영하지 않는다 | 목록 화면으로 이동한다 | 수정 내용이 있으면 확인 안내를 표시한다 |

## 5. 사용자 시나리오

### 정상

- 이미지 등록 → 결과가 화면에 바로 반영된다.
- 연결 URL 입력 → 결과가 화면에 바로 반영된다.
- 게시 기간 설정 → 결과가 화면에 바로 반영된다.
- 미리보기 조회 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 이미지 등록 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 연결 URL 입력 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 게시 기간 설정 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 미리보기 조회 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 이미지 비율이 기준과 다르면 등록할 수 없습니다. 고객 화면에서 절삭됩니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 이미지 비율이 21:9 가 아니면 등록하지 않고 사유를 표시합니다.
- 시작일이 종료일보다 이후일 수 없습니다.

### 화면 정책

_N/A — 이 화면에는 해당되지 않습니다._

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 모바일 전용 이미지

## 관련 문서

- [화면 캡처](/docs/page-view/banners-new) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
