# Interior

> F&B Client 템플릿 A · `/interior` · id `interior`
> 원본: `apps/fnb-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Interior |
| 메뉴 위치 | 인테리어 > 인테리어 |
| URL | `/interior` |
| 화면 목적 | 평형별로 얼마 들고 몇 석이 나오는지 보인다. |
| 기대 효과 | 상담 전에 스스로 두들겨 보게 해서, 전화가 얼마 드느냐로 시작하지 않게 한다. |
| 어드민 연동 | 창업 > 비용 · 절차 (평당 단가) |
| Figma 페이지 순번 | 25 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | 내 평수에 얼마 드나와 평당 단가 · 빠진 것 | — |
| 평형별 안 | 한 장이 폭을 다 쓰는 굴림판 — 사진 자리 · 값 · 좌석 · 공사 주 · 맞는 자리 | — |
| 사진 모자이크 | 여덟 칸. 숫자를 읽은 다음에 온다 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 평형별 안 넘겨 보기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 좌석 수 · 공사 기간 확인 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 완성 매장 사진 모자이크 훑기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 평형별 안 넘겨 보기 → 결과가 화면에 바로 반영된다.
- 좌석 수 · 공사 기간 확인 → 결과가 화면에 바로 반영된다.
- 완성 매장 사진 모자이크 훑기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 평형별 안 넘겨 보기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 좌석 수 · 공사 기간 확인 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 완성 매장 사진 모자이크 훑기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 공사비를 표에 적지 않는다 — 평당 단가 하나에서 세 값이 나온다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 평당 단가 하나에서 세 값이 나온다. 창업 비용표의 인테리어 줄도 같은 값이다.
- 평수와 함께 ㎡ 를 적는다 — 자리를 보러 다니는 사람은 부동산에서 ㎡ 로 듣는다.
- 철거 · 소방 · 외부 간판은 빠진다는 것을 제목 줄에 적는다.
- 사진은 여덟의 배수로 둔다. 아홉째가 새 묶음을 열면 그 줄부터 격자에 구멍이 생긴다.
- 맨 아래에 상담 판을 두지 않는다 — 갈 길은 헤더 · 푸터 · 고정 상담 바에 그대로 있다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 완성 매장 사진. 지금은 문어 그림 자리다.
- 평형을 직접 넣어 계산해 보는 칸.

## 관련 문서

- [화면 캡처](/docs/page-view/interior) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
