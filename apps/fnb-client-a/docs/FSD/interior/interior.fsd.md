# Interior

> F&B Client 템플릿 A · `/interior` · id `interior`
> 원본: `apps/fnb-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Interior |
| 메뉴 위치 | 인테리어 > 인테리어 |
| URL | `/interior` |
| 화면 목적 | 평형별 인테리어 비용과 좌석 수, 공사 기간을 안내하는 인테리어 정보 페이지입니다. |
| 배경 | — |
| 기대 효과 | 창업 검토자가 상담 이전에 예상 비용을 확인할 수 있습니다. |
| 어드민 연동 | 창업 > 비용 · 절차 (평당 단가) |
| Figma 페이지 순번 | 25 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 페이지 제목 | 평당 단가와 비용 산정 제외 항목 안내 | — |
| 평형별 인테리어 안 | 이미지 · 예상 비용 · 좌석 수 · 공사 기간 · 권장 입지로 구성한 슬라이드 | — |
| 시공 사례 이미지 | 완성 매장 이미지 8개 항목 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 처리 | 결과 |
|---|---|---|---|
| 평형별 인테리어 안 조회 기능 제공 | 평형별 인테리어 안 조회 | 등록된 데이터를 조회한다 | 조회한 정보를 화면에 표시한다 |
| 좌석 수 및 공사 기간 조회 기능 제공 | 좌석 수 및 공사 기간 조회 | 등록된 데이터를 조회한다 | 조회한 정보를 화면에 표시한다 |
| 시공 사례 이미지 조회 기능 제공 | 시공 사례 이미지 조회 | 등록된 데이터를 조회한다 | 조회한 정보를 화면에 표시한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 평형별 인테리어 안 조회 → 결과가 화면에 바로 반영된다.
- 좌석 수 및 공사 기간 조회 → 결과가 화면에 바로 반영된다.
- 시공 사례 이미지 조회 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 평형별 인테리어 안 조회 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 좌석 수 및 공사 기간 조회 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 시공 사례 이미지 조회 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 공사비를 개별 항목으로 입력받지 않습니다. 평당 단가에서 산출합니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 평당 단가 1개 값에서 평형별 3개 값을 산출합니다. 창업 비용표의 인테리어 항목도 동일한 값을 사용합니다.
- 평수와 함께 ㎡ 단위를 병기합니다.
- 철거 · 소방 · 외부 간판이 비용에서 제외된다는 사실을 제목 영역에 표시합니다.
- 시공 사례 이미지는 8의 배수로 등록합니다.
- 페이지 하단에 별도 상담 영역을 배치하지 않습니다. 상단 내비게이션 · 푸터 · 고정 상담 바에서 상담 경로를 제공합니다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 실제 시공 사례 이미지 등록.
- 평형 직접 입력 방식의 비용 계산 기능.

## 관련 문서

- [화면 캡처](/docs/page-view/interior) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
