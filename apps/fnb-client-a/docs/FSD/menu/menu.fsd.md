# Menu

> F&B Client 템플릿 A · `/menu` · id `menu`
> 원본: `apps/fnb-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Menu |
| 메뉴 위치 | 메뉴 > 메뉴판 |
| URL | `/menu` |
| 화면 목적 | 파는 것을 묶음별로 훑고 한 가지를 고른다. |
| 기대 효과 | 스무 가지를 한 줄로 세우지 않고 묶음 하나를 고르게 한다. |
| 어드민 연동 | 등록 > 메뉴 (목록 · 등록 · 상세) · 등록 > 메뉴 > 메뉴 묶음 |
| Figma 페이지 순번 | 20 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | 그날 들어온 문어만 쓴다는 한 줄과 부가세 안내 | — |
| 왼쪽 기둥 | 묶음 고르기 — 옆에 몇 가지인지 함께 | — |
| 묶음 설명 | 고른 묶음의 한 줄이 카드 위에 선다 | 묶음에 설명이 있을 때 |
| 메뉴 격자 | 사진 자리 · 표 · 이름 · 설명 · 값 | — |
| 알레르기 안내 | 메뉴판 아래 한 번. 줄마다 적으면 같은 문장이 스무 번 선다 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 왼쪽 기둥에서 묶음 고르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 메뉴 카드 열기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 묶음 | 메뉴판의 갈래 | 선택 | 선택 | 어드민이 정한 묶음 중 비어 있지 않은 것 | 문어숙회 |

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 왼쪽 기둥에서 묶음 고르기 → 결과가 화면에 바로 반영된다.
- 메뉴 카드 열기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 왼쪽 기둥에서 묶음 고르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 메뉴 카드 열기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 내려 둔 메뉴는 목록에 오지 않는다 — 품절과 계절 메뉴가 여기서 갈린다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 비어 있는 묶음은 기둥에 세우지 않는다. 눌렀을 때 빈 화면이 나오면 없어진 줄 안다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 차례는 어드민이 정한 묶음 순서이고, 묶음 안은 메뉴 등록 순서다.
- 고른 묶음은 주소에 남기지 않는다 — 한 화면에서 기둥을 눌러 가며 보는 자리라 공유할 조건이 아니다.
- 값은 부가세가 포함된 금액이다.
- 내려 둔 메뉴는 값이 남되 손님에게는 없는 메뉴다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 메뉴 사진. 지금은 문어 그림 자리(`PhotoSlot`)가 대신한다.
- 알레르기로 거르기. 지금은 상세에서 하나씩 확인한다.

## 관련 문서

- [화면 캡처](/docs/page-view/menu) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
