# Brand

> F&B Client 템플릿 A · `/brand` · id `brand`
> 원본: `apps/fnb-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Brand |
| 메뉴 위치 | 브랜드 > 브랜드 이야기 |
| URL | `/brand` |
| 화면 목적 | 무엇을 지키는 집인지 읽습니다. |
| 배경 | — |
| 기대 효과 | 연도가 아니라 매일 지키는 것으로 브랜드를 설명합니다. |
| 어드민 연동 | 설정 > 브랜드 정보 · 등록 > 가맹점 (매장 수) |
| Figma 페이지 순번 | 10 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | 브랜드 이야기와 한 줄 소개 | — |
| 여는 글 | 언제 어디서 시작했고 지금 몇 곳인지 | — |
| 지키는 것 | 원칙 셋 — 번호 · 제목 · 두어 줄 | — |
| 매장으로 보내는 칸 | 읽고 나서 가 볼 곳 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 지키는 것 셋 읽기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 매장 찾기로 이동 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 지키는 것 셋 읽기 → 결과가 화면에 바로 반영된다.
- 매장 찾기로 이동 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 지키는 것 셋 읽기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 매장 찾기로 이동 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 연혁 표를 세우지 않는다 — 그 표를 읽는 사람이 이 사이트에 오지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 원칙 셋은 화면이 들고 있다. 어드민에서 고칠 수 있게 두면 계절 행사 문구로 덮인다.
- 매장 수는 문 연 곳만 센다 — 목록 · 홈 · 여기 셋이 같은 답이어야 한다.
- 원칙은 셋으로 고정한다. 넷째부터는 앞의 셋을 다시 말하는 문장이 된다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 연혁. 필요해지는 것은 상장을 준비하는 때이고, 그때 필요한 것은 이 화면이 아니라 IR 템플릿이다.
- 본점 사진 · 대표 인사말.

## 관련 문서

- [화면 캡처](/docs/page-view/brand) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
