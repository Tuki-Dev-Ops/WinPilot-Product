# Notices

> F&B Client 템플릿 A · `/support/notices` · id `support-notices`
> 원본: `apps/fnb-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Notices |
| 메뉴 위치 | 고객센터 > 공지사항 |
| URL | `/support/notices` |
| 화면 목적 | 값이 바뀌거나 매장이 열고 닫히는 일을 묻기 전에 읽힌다. |
| 기대 효과 | 제목만 훑고 읽을 것 하나만 펴므로, 넷을 읽으려고 네 번 들어갔다 나오지 않는다. |
| 어드민 연동 | 고객센터 > 공지사항 |
| Figma 페이지 순번 | 50 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | 먼저 알려 드린다는 한 줄 | — |
| 왼쪽 기둥 | 공지사항 · 자주 묻는 질문 | — |
| 접힌 목록 | 고정 딱지 · 제목 · 날짜. 눌러서 본문을 편다 | — |
| 빈 상태 | 아직 올라온 공지가 없을 때 | 목록 0건 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 왼쪽 기둥에서 자주 묻는 질문으로 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 공지 펴 보기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 왼쪽 기둥에서 자주 묻는 질문으로 → 결과가 화면에 바로 반영된다.
- 공지 펴 보기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 왼쪽 기둥에서 자주 묻는 질문으로 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 공지 펴 보기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 숨긴 공지는 오지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 한 번에 하나만 열린다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 공지가 하나도 없으면 빈 상태 한 줄을 세운다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 차례는 store 가 세운다 — 고정한 것이 먼저, 그다음 날짜 역순. 홈의 공지 띠가 같은 차례를 읽는다.
- 맨 위 하나는 펴 둔 채로 들어온다. 그 자리는 붙여 둔 글이 오는 곳이다.
- 상세 화면이 없어 글 하나의 주소도 없다. 지금 공지의 성격에서 링크로 공유하는 일이 실제로 일어나지 않는다.
- 고정은 어드민이 정한다. 켜 두고 잊으면 반년 지난 글이 첫 줄에 남는다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 공지 상세 화면과 주소. 공유가 필요해지는 날 만든다.
- 첨부파일.
- 공지 검색.

## 관련 문서

- [화면 캡처](/docs/page-view/support-notices) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
