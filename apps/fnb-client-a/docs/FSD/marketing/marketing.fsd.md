# Marketing

> F&B Client 템플릿 A · `/marketing` · id `marketing`
> 원본: `apps/fnb-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Marketing |
| 메뉴 위치 | 마케팅 > 마케팅 |
| URL | `/marketing` |
| 화면 목적 | 본사가 밖에서 무엇을 하고 있는지를 올린 것으로 보입니다. |
| 배경 | 정보공개서가 항목마다 답하고 있어, 같은 내용을 두 곳에서 서로 다르게 적을 위험이 있었습니다. |
| 기대 효과 | 잘 돌고 있다는 말을 하지 않고 올라간 것으로 보입니다. |
| 어드민 연동 | 등록 > 마케팅 (창구 · 글) |
| Figma 페이지 순번 | 26 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | 본사가 하는 것을 그대로 | — |
| 왼쪽 기둥 | 창구 고르기 — 여기만 개수를 적지 않는다 | — |
| 창구 설명 | 무엇을 올리는 곳인지 한 줄 | — |
| 글 격자 | 사진 자리 · 제목 · 한 줄 · 올린 날 | — |
| 빈 상태 | 아직 올라온 글이 없을 때 | 고른 창구의 글이 0건 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 왼쪽 기둥에서 창구 고르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 올라간 글 훑기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 창구 | 어느 계정의 글을 볼지 | 선택 | 선택 | 어드민이 등록한 창구 | 인스타그램 |

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 왼쪽 기둥에서 창구 고르기 → 결과가 화면에 바로 반영된다.
- 올라간 글 훑기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 왼쪽 기둥에서 창구 고르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 올라간 글 훑기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 글이 없는 창구는 빈 상태 한 줄을 세운다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 카드는 누를 수 없다 — 계정 주소를 아직 갖고 있지 않다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 창구는 실제로 도는 것만 세운다. 비어 있는 창구는 없는 창구보다 나쁘다.
- 기둥에 창구 이름만 적는다. 계정 이름을 함께 적으면 누를 수 없는 주소가 눌러 보고 싶게 생긴 자리에 선다.
- 올린 날을 늘 적는다 — 마케팅 창구에서 먼저 재는 것이 최근 것이 있는가다.
- 창구를 고를 때 재는 것이 글 수가 아니라 어떤 곳인가라, 개수를 적지 않는다.
- 맨 아래에 상담 판을 두지 않는다. 인테리어와 같은 판단이다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 계정 주소. 열리는 날 카드가 링크가 되고 기둥에 계정 이름이 함께 선다.
- 글 사진. 지금은 문어 그림 자리다.

## 관련 문서

- [화면 캡처](/docs/page-view/marketing) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
