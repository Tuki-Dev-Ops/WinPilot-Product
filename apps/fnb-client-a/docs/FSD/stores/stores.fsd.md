# Store Finder

> F&B Client 템플릿 A · `/stores` · id `stores`
> 원본: `apps/fnb-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Store Finder |
| 메뉴 위치 | 매장안내 > 매장 찾기 |
| URL | `/stores` |
| 화면 목적 | 가까운 매장을 찾고 거기까지 어떻게 가는지 본다. |
| 기대 효과 | 지도가 판을 채우고 찾는 자리가 그 위에 떠서, 훑다가 하나를 고르는 일이 한 화면에서 끝난다. |
| 어드민 연동 | 등록 > 가맹점 (주소 · 번호 · 시간 · 되는 것 · 상태 · 좌표) |
| Figma 페이지 순번 | 30 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 지도 | 판 전체를 채운다. 좁은 화면에서는 위에 눕는다 | — |
| 찾는 자리 | 검색창 · 지역 고르개 · 결과 수 · 조건 지우기. 지도 위 왼쪽에 뜬다 | — |
| 결과 목록 | 이름 · 상태 · 주소 · 시간 · 번호 · 되는 것. 안에서 굴린다 | — |
| 빈 상태 | 조건에 맞는 매장이 없을 때 — 새 매장은 공지사항에 먼저 올린다고 적는다 | 결과 0건 |
| 지도 설명 판 | 무엇이 없어서 안 뜨는지와 어디에 넣으면 되는지 | 열쇠가 없거나 SDK 가 실패했을 때 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 매장명 · 주소로 검색 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 지역으로 좁히기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 줄을 눌러 지도 당기기(다시 누르면 놓는다) | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 지도 표식 누르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 조건 지우기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 검색어 | 매장명 · 주소 · 지역 일부 | 텍스트 | 선택 | 앞뒤 공백 제거 · 대소문자 가리지 않음 | 역삼 |
| 지역 | 권역으로 좁히기 | 선택 | 선택 | 매장 목록에서 뽑은 권역. 옆에 몇 곳인지 적는다 | 수도권 |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 조건 지우기 | 검색어 · 지역 · 고른 매장을 모두 놓는다 | 전체 매장으로 돌아가고 지도가 다시 다 담기게 맞춰진다 | — |
| 매장 줄 | 그 매장으로 지도를 당긴다 | 지도가 4단계로 확대되고 그 자리로 움직인다 | 이미 고른 줄이면 놓고 전체로 돌아간다 |

## 5. 사용자 시나리오

### 정상

- 매장명 · 주소로 검색 → 결과가 화면에 바로 반영된다.
- 지역으로 좁히기 → 결과가 화면에 바로 반영된다.
- 줄을 눌러 지도 당기기(다시 누르면 놓는다) → 결과가 화면에 바로 반영된다.
- 지도 표식 누르기 → 결과가 화면에 바로 반영된다.
- 조건 지우기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 매장명 · 주소로 검색 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 지역으로 좁히기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 줄을 눌러 지도 당기기(다시 누르면 놓는다) **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 지도 표식 누르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 조건 지우기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 휴점 매장은 목록에 오지 않는다 — 값은 남고 사이트에서만 빠진다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 준비중 매장은 번호 자리에 여는 달을 적는다. 없는 번호를 적어 두면 그리로 전화가 간다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 지도 열쇠가 없거나 도메인이 등록되지 않으면 빈 상자 대신 설명 판을 세운다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 거르개와 지도가 이어져 있다. 목록이 좁혀지면 지도도 그만큼 좁혀진다.
- 고른 매장이 없으면 지금 목록에 선 매장 전부가 담기게 맞춘다.
- 조건 지우기는 건 조건이 있을 때만 뜬다 — 늘 서 있으면 지울 것이 없는데도 누르게 된다.
- 지역 목록은 매장에서 뽑는다. 손으로 적으면 새 지역의 매장이 어느 거르개로도 걸리지 않는다.
- 좌표는 매장 값이 들고 있다. 화면에서 주소를 좌표로 바꾸면 매장 수만큼 요청이 나가고, 하나 실패하면 그 표식만 조용히 사라진다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 매장 상세 화면. 지금은 줄 하나가 다 적어 더 볼 것이 없다.
- 내 위치에서 가까운 순.
- 지금 문을 열었는지 여부.

## 관련 문서

- [화면 캡처](/docs/page-view/stores) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
