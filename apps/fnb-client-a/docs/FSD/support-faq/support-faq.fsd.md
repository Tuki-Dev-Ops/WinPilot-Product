# FAQ

> F&B Client 템플릿 A · `/support/faq` · id `support-faq`
> 원본: `apps/fnb-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | FAQ |
| 메뉴 위치 | 고객센터 > 자주 묻는 질문 |
| URL | `/support/faq` |
| 화면 목적 | 드시러 오시는 분이 자주 묻는 것을 한 자리에 모읍니다. |
| 배경 | — |
| 기대 효과 | 누구인지를 고르는 줄이 없어 들어오자마자 물음이 바로 섭니다. |
| 어드민 연동 | 고객센터 > FAQ |
| Figma 페이지 순번 | 51 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | 창업 물음은 창업 안내에서 따로 받는다는 안내와 함께 | — |
| 왼쪽 기둥 | 공지사항 · 자주 묻는 질문 | — |
| 접힌 목록 | 물음만 세로로 늘어서고 하나를 편다 | — |
| 빈 상태 | 아직 올라온 질문이 없을 때 | 목록 0건 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 왼쪽 기둥에서 공지사항으로 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 물음 펴 보기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 왼쪽 기둥에서 공지사항으로 → 결과가 화면에 바로 반영된다.
- 물음 펴 보기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 왼쪽 기둥에서 공지사항으로 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 물음 펴 보기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 숨긴 물음은 오지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 한 번에 하나만 열린다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 물음이 하나도 없으면 빈 상태 한 줄을 세운다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 드시러 오시는 분의 물음만 선다. 차리려는 분의 물음은 창업안내 아래 자기 화면이 있다.
- 누구인지를 고르는 줄을 두지 않는다 — 같은 것을 두 곳에서 다르게 보여 주면 어느 쪽이 다 있는 목록인지 알 수 없다.
- 여기에는 분류를 세우지 않는다. 손님 물음은 개수가 적어 기둥을 하나 더 두면 고르는 자리만 늘어난다.
- 접기 동작은 공지사항 · 창업 개설문의와 같은 조각을 쓴다 — 한 번 배운 사람이 다시 배우지 않는다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 물음 검색.
- 분류. 물음이 늘면 창업 개설문의처럼 왼쪽 기둥이 하나 더 선다.

## 관련 문서

- [화면 캡처](/docs/page-view/support-faq) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
