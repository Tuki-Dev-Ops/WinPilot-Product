# Menu Categories

> F&B Admin · `/menus/categories` · id `menus-categories`
> 원본: `apps/fnb-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Menu Categories |
| 메뉴 위치 | 등록 > (목록에서 진입) |
| URL | `/menus/categories` |
| 화면 목적 | 묶음마다 몇 가지가 서 있는지 본다. |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 메뉴판 `/menu` — 이 순서대로 세로로 이어 붙는다. |
| Figma 페이지 순번 | 13 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 먹는 순서 | 주된 것 → 곁들임 → 마실 것 | — |
| 알아 둘 것 | 묶음을 늘리면 무엇이 생기는지 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 먹는 순서대로 늘어선 묶음을 읽는다 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 묶음별 판매중·내림 가짓수를 센다 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 먹는 순서대로 늘어선 묶음을 읽는다 → 결과가 화면에 바로 반영된다.
- 묶음별 판매중·내림 가짓수를 센다 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 먹는 순서대로 늘어선 묶음을 읽는다 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 묶음별 판매중·내림 가짓수를 센다 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 순서를 바꾸는 자리를 두지 않는다 — 넷의 차례는 취향이 아니라 먹는 순서다. 바꾸면 메뉴판이 없는 흐름을 그린다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 묶음을 늘리고 줄이는 일도 여기서 하지 않는다. 묶음이 하나 늘면 메뉴판 칸이 하나 늘고, 넣을 메뉴가 없으면 빈 제목만 선다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 파는 것과 내려 둔 것을 함께 적는다. 파는 것만 적으면 내려 둔 것이 있다는 사실이 사라지고, 그때 그 메뉴는 아무도 다시 열지 않는다.
- 읽기 전용임을 배지로 적는다 — 고칠 수 없는 화면과 아직 안 만든 화면은 다른 말이다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 묶음 늘리기와 순서 바꾸기 — 메뉴판 화면을 함께 손볼 때.

## 관련 문서

- [화면 캡처](/docs/page-view/menus-categories) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
