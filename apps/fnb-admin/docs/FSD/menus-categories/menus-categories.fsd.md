# Menu Categories

> F&B Admin · `/menus/categories` · id `menus-categories`
> 원본: `apps/fnb-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Menu Categories |
| 메뉴 위치 | 등록 > (목록에서 진입) |
| URL | `/menus/categories` |
| 화면 목적 | 메뉴 카테고리 구성과 카테고리별 등록 건수를 조회하는 페이지입니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 메뉴 목록 `/menu` — 이 순서대로 카테고리가 배치됩니다. |
| Figma 페이지 순번 | 13 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 카테고리 목록 | 노출 순서대로 배치한 카테고리와 등록 건수 | — |
| 운영 안내 영역 | 카테고리 변경 시 고객 사이트에 발생하는 영향 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 처리 | 결과 |
|---|---|---|---|
| 카테고리 구성 및 노출 순서 조회 기능 제공 | 카테고리 구성 및 노출 순서 조회 | 등록된 데이터를 조회한다 | 조회한 정보를 화면에 표시한다 |
| 카테고리별 판매중 · 비노출 건수 조회 기능 제공 | 카테고리별 판매중 · 비노출 건수 조회 | 등록된 데이터를 조회한다 | 조회한 정보를 화면에 표시한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 카테고리 구성 및 노출 순서 조회 → 결과가 화면에 바로 반영된다.
- 카테고리별 판매중 · 비노출 건수 조회 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 카테고리 구성 및 노출 순서 조회 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 카테고리별 판매중 · 비노출 건수 조회 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 카테고리 순서 변경 기능은 제공하지 않습니다. 순서가 고객 사이트 메뉴 목록의 구성과 직결되기 때문입니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 카테고리 추가 및 삭제 기능은 제공하지 않습니다. 카테고리 증가 시 메뉴가 없는 빈 영역이 발생합니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 판매중 건수와 비노출 건수를 함께 표시합니다.
- 읽기 전용 화면임을 배지로 표시합니다. 수정 불가 화면과 미구현 화면은 구분되어야 합니다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 카테고리 추가 및 순서 변경. 메뉴 목록 화면과 함께 개선합니다.

## 관련 문서

- [화면 캡처](/docs/page-view/menus-categories) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
