# Dashboard

> F&B Admin · `/` · id `dashboard`
> 원본: `apps/fnb-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Dashboard |
| 메뉴 위치 | 대시보드 |
| URL | `/` |
| 화면 목적 | 오늘 손대야 할 것 하나를 먼저 보여 줍니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 없음 — 운영자만 보는 화면이다. 다만 여기 수치는 전부 사이트에 나가는 값에서 나온다. |
| Figma 페이지 순번 | 1 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 요약 카드 | 밀린 것 · 지금 사이트에 걸린 것 | — |
| 답을 기다리는 창업 문의 | 접수된 채로 남아 있는 건 | — |
| 사이트에 걸린 공지 | 손님이 지금 읽고 있는 것 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 요약 카드 여섯으로 오늘 수치를 훑는다 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 답을 기다리는 창업 문의를 바로 연다 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 사이트에 지금 걸린 공지를 확인한다 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 요약 카드 여섯으로 오늘 수치를 훑는다 → 결과가 화면에 바로 반영된다.
- 답을 기다리는 창업 문의를 바로 연다 → 결과가 화면에 바로 반영된다.
- 사이트에 지금 걸린 공지를 확인한다 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 요약 카드 여섯으로 오늘 수치를 훑는다 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 답을 기다리는 창업 문의를 바로 연다 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 사이트에 지금 걸린 공지를 확인한다 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 매출 숫자를 두지 않는다 — 매장 매출은 POS 와 배달앱이 갖고 있어, 옮겨 오면 두 곳의 숫자가 다른 상태가 만들어진다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 색은 두 카드에만 붙는다. 답하지 않은 창업 문의와 셋을 넘긴 팝업 — 늘 붉으면 붉은 것이 뜻을 잃는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 밀린 문의는 오래된 것이 위다 — 늦은 차례가 곧 지키지 못한 약속의 차례다.
- 오늘을 화면이 정해서 넘긴다. 조각 안에서 `new Date()` 를 부르면 미리 만들어 두는 화면에서 그 값이 빌드한 날에 굳는다.
- 배너·팝업이 지금 걸렸는지는 `bannerState()` 하나가 정한다. 화면마다 계산하면 목록과 대시보드가 다른 답을 낸다.
- 팝업 카드는 셋을 넘길 때만 색이 붙는다 — 셋을 넘으면 손님이 팝업 셋을 닫고서야 첫 화면에 닿는데, 그 상태는 올린 사람 눈에 안 보인다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- POS 를 읽어 오는 방식의 매출 보기.

## 관련 문서

- [화면 캡처](/docs/page-view/dashboard) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
