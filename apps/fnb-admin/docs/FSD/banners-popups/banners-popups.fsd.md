# Popups

> F&B Admin · `/banners/popups` · id `banners-popups`
> 원본: `apps/fnb-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Popups |
| 메뉴 위치 | 배너 > 팝업 |
| URL | `/banners/popups` |
| 화면 목적 | 고객 사이트 팝업을 관리하는 목록 페이지입니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 홈 팝업 영역 — 현재 고객 사이트는 본 데이터를 사용하지 않습니다. 적용 시점에 연동합니다. |
| Figma 페이지 순번 | 75 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 목록 검색 영역 | 검색 입력 · 노출 상태 · 오늘 하루 보지 않기 · 팝업 등록 버튼 | — |
| 목록 테이블 | 1행에 팝업 1건. 제목 하단에 닫기 방식을 함께 표시 | — |
| 선택 항목 처리 영역 | 선택한 항목에 대한 일괄 처리 | 목록에서 항목을 선택했을 때 |
| 데이터 없음 안내 | 검색 조건에 해당하는 항목이 없을 때 표시 | 검색 결과 0건 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 처리 | 결과 |
|---|---|---|---|
| 제목 및 내용 검색 기능 제공 | 제목 및 내용 검색 | 입력한 검색어를 조회 조건에 반영하여 목록을 다시 조회한다 | 조건에 해당하는 목록과 전체 건수를 표시한다 |
| 노출 상태 및 오늘 하루 보지 않기 설정 필터 기능 제공 | 노출 상태 및 오늘 하루 보지 않기 설정 필터 | 선택한 조건으로 목록을 필터링한다 | 조건에 해당하는 항목만 표시한다 |
| 목록 항목 선택 시 상세 페이지 이동 기능 제공 | 목록 항목 선택 시 상세 페이지로 이동 | 선택한 항목의 식별값으로 대상 화면을 조회한다 | 해당 상세 화면으로 이동한다 |
| 팝업 등록 기능 제공 | 팝업 등록 | 입력값을 검증한 뒤 신규 항목으로 저장한다 | 저장 결과를 안내하고 목록 화면으로 이동한다 |
| 팝업 삭제 기능 제공 | 팝업 삭제 | 삭제 여부를 확인한 뒤 대상 항목을 삭제 처리한다 | 목록에서 제외하고 처리 결과를 안내한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 제목 및 내용 검색 → 결과가 화면에 바로 반영된다.
- 노출 상태 및 오늘 하루 보지 않기 설정 필터 → 결과가 화면에 바로 반영된다.
- 목록 항목 선택 시 상세 페이지로 이동 → 결과가 화면에 바로 반영된다.
- 팝업 등록 → 결과가 화면에 바로 반영된다.
- 팝업 삭제 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 제목 및 내용 검색 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 노출 상태 및 오늘 하루 보지 않기 설정 필터 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 목록 항목 선택 시 상세 페이지로 이동 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 팝업 등록 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 팝업 삭제 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 팝업은 배너와 별도 목록으로 관리합니다. 팝업은 닫기 조작을 요구하므로 배너와 노출 강도가 다릅니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 오늘 하루 보지 않기를 비활성화하면 방문 시마다 노출됩니다. 목록에서 해당 설정을 확인할 수 있어야 합니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 동시 게시 팝업은 3건을 초과하지 않도록 관리합니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 오늘 하루 보지 않기 설정을 별도 열로 표시합니다. 반복 노출이 필요한 공지에만 사용하는 설정이기 때문입니다.
- 배너 목록과 동일한 상태 색상 체계를 사용합니다.
- 기준일은 화면에서 결정하여 전달합니다.
- 게시 기간이 중복되면 목록 상단 항목부터 노출합니다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 노출 순서 변경.

## 관련 문서

- [화면 캡처](/docs/page-view/banners-popups) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
