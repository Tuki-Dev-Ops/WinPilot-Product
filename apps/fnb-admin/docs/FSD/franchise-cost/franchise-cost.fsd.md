# Franchise Cost

> F&B Admin · `/settings/franchise` · id `franchise-cost`
> 원본: `apps/fnb-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Franchise Cost |
| 메뉴 위치 | 창업 > 비용 · 절차 |
| URL | `/settings/franchise` |
| 화면 목적 | 사이트 창업 안내에 지금 무엇이 적혀 있는지 한자리에서 봅니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 창업안내 `/franchise` — 비용표와 절차가 그대로 선다. · 인테리어 `/interior` — 평당 단가 하나에서 평형별 세 값이 함께 나온다. |
| Figma 페이지 순번 | 38 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 창업 비용 | 항목 · 금액 · 설명과 합계 | — |
| 개점까지 | 상담 신청부터 시식 영업까지의 차례와 기간 | — |
| 알아 둘 것 | 왜 여기서 고치지 않는지 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 비용 항목과 합계 읽기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 개점까지의 차례와 기간 읽기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 비용 항목과 합계 읽기 → 결과가 화면에 바로 반영된다.
- 개점까지의 차례와 기간 읽기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 비용 항목과 합계 읽기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 개점까지의 차례와 기간 읽기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 여기서 고치지 못한다. 창업 비용과 절차는 정보공개서에 신고한 값이라, 사이트에 적힌 값과 신고한 값이 다르면 문구 오류가 아니라 가맹사업법 문제가 된다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 숫자만 고칠 수 있게 두면 담당자가 신고 절차를 모른 채 고치게 되고, 그 사실은 분쟁이 나서야 드러난다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 합계와 빠진 것을 같은 줄에 둔다. 사이트에서도 같은 모양이라, 전화를 받는 사람이 신청자와 같은 것을 보게 된다.
- 값을 바꾸는 일은 신고를 함께 갱신할 때 코드에서 한다.
- 목록이 아니라 값 한 벌이라 등록 화면이 없다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 신고 이력과 함께 고치는 자리.

## 관련 문서

- [화면 캡처](/docs/page-view/franchise-cost) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
