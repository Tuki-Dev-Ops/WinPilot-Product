# Popup Detail

> F&B Admin · `/banners/popups/[popupId]` · id `banners-popups-detail`
> 원본: `apps/fnb-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Popup Detail |
| 메뉴 위치 | 배너 > (목록에서 진입) |
| URL | `/banners/popups/[popupId]` · 예 `/banners/popups/PU-01` |
| 화면 목적 | 이미 띄운 팝업의 말과 닫는 방법을 고칩니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 홈에 뜨는 팝업 자리 — 실제로 띄우는 날 이 값이 그 자리에 온다. |
| Figma 페이지 순번 | 76 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 무슨 팝업인가 | 팝업 번호 · 제목 · 내용 | — |
| 언제 · 어떻게 뜨나 | 시작 · 종료 · 하루 감추기 · 노출 | — |
| 저장 줄 | 저장 | — |
| 확인 창 | 제목 · 기간 · 매번 뜨는지를 한 줄로 다시 읽는다 | 저장을 눌렀을 때 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 제목·내용 고치기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 기간 고치기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 하루 감추기 끄고 켜기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 노출 끄고 켜기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 팝업 번호 | 팝업을 가리키는 번호 | 읽기 전용 | 선택 | — | PU-01 |
| 제목 | 팝업 맨 위 한 줄 | 텍스트 | 필수 | — | — |
| 내용 | 한눈에 읽혀야 하는 글 | 여러 줄 | 필수 | 120자 안이면 한눈에 읽힌다 | — |
| 시작 | 이 날부터 뜬다 | 텍스트 | 필수 | `YYYY-MM-DD` | — |
| 종료 | 비우면 상시 | 텍스트 | 선택 | — | — |
| 하루 감추기 | 끄면 올 때마다 뜬다 | 스위치 | 선택 | — | — |
| 노출 | 끄면 기간과 상관없이 즉시 내려간다 | 스위치 | 선택 | — | — |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 저장 | 입력을 확인하고 확인 창을 연다 | 토스트로 제목 · 기간 · 매번 뜨는지를 알린다 | 확인이 필요한 칸의 개수와 이름을 토스트에 적는다 |

## 5. 사용자 시나리오

### 정상

- 제목·내용 고치기 → 결과가 화면에 바로 반영된다.
- 기간 고치기 → 결과가 화면에 바로 반영된다.
- 하루 감추기 끄고 켜기 → 결과가 화면에 바로 반영된다.
- 노출 끄고 켜기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 제목·내용 고치기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 기간 고치기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 하루 감추기 끄고 켜기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 노출 끄고 켜기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| `하루 감추기` 를 끄는 것이 이 화면에서 가장 무거운 결정이다. 끄면 올 때마다 뜨고, 그 상태로 몇 달이 지나면 사람들은 팝업을 읽지 않고 닫는 습관을 갖는다 — 그러면 정작 읽혀야 할 다음 팝업도 안 읽힌다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 막지는 않는다. 휴점과 값 조정처럼 정말 매번 보여야 하는 것이 있다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 팝업 번호는 고칠 수 없다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 제목과 내용은 공백만으로 둘 수 없다.
- 시작은 `YYYY-MM-DD` 모양이어야 한다. 종료는 비우거나 같은 모양이어야 한다.
- 종료가 시작보다 앞이면 알린다 — 그대로면 뜨지 않는다는 말을 함께 적는다.
- 내용이 120자를 넘으면 세어 알리되 막지 않는다.

### 화면 정책

- 내용을 짧게 받는다. 팝업은 읽는 것을 막는 자리라, 길면 읽지 않고 닫는다.
- 켜고 끄는 자리 옆에 그 말을 그대로 적어 둔다 — 무거운 결정은 무겁게 읽혀야 한다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 뜨는 자리와 크기 정하기.

## 관련 문서

- [화면 캡처](/docs/page-view/banners-popups-detail) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
