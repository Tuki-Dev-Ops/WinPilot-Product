# Inquiry Detail

> F&B Admin · `/inquiries/[inquiryId]` · id `inquiries-detail`
> 원본: `apps/fnb-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Inquiry Detail |
| 메뉴 위치 | 창업 > (목록에서 진입) |
| URL | `/inquiries/[inquiryId]` · 예 `/inquiries/FQ-0108` |
| 화면 목적 | 문의 한 건을 읽고 통화에서 알게 된 것을 쌓는다. |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 없음 — 상태도 메모도 사이트에 나가지 않는다. 대시보드가 이 상태로 밀린 건을 센다. |
| Figma 페이지 순번 | 31 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 신청자가 남긴 것 | 접수번호 · 받은 날 · 성함 · 연락처 · 지역 · 예산 · 하고 싶은 말 | — |
| 상담 기록 | 상태와 메모 | — |
| 저장 줄 | 저장 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 신청자가 남긴 것 읽기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 상태 바꾸기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 상담 메모 쓰기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 상태 | 접수 · 상담중 · 완료 · 보류 | 선택 | 선택 | 목록 화면이 갖는 넷 중 하나 | — |
| 상담 메모 | 다음에 이 건을 여는 사람이 읽는다 | 여러 줄 | 선택 | 무엇을 약속했는지까지 적는다 | — |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 저장 | 상태와 메모를 함께 반영한다 | 토스트로 신청자와 상태를 알린다 | — |

## 5. 사용자 시나리오

### 정상

- 신청자가 남긴 것 읽기 → 결과가 화면에 바로 반영된다.
- 상태 바꾸기 → 결과가 화면에 바로 반영된다.
- 상담 메모 쓰기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 신청자가 남긴 것 읽기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 상태 바꾸기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 상담 메모 쓰기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 신청자가 남긴 것은 못 고친다. 고칠 수 있게 두면 통화 중에 들은 것을 덮어쓰게 되고, 그 순간 원본이 사라진다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 통화에서 알게 된 것은 상담 메모에 쌓는다 — 칸이 따로 있어야 원본과 우리가 적은 것이 갈린다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 상태와 메모가 한 카드에 있다. 상태를 바꾸는 일은 대개 메모를 적은 직후에 일어나므로, 카드를 나누면 저장을 두 번 눌러야 하고 그러다 한쪽만 저장한 채 화면을 떠난다.
- 등록 화면이 없다. 이 자원은 밖에서만 생긴다 — 어드민에서 만들 수 있게 두면 신청자가 직접 남긴 것과 우리가 옮겨 적은 것이 한 목록에 섞인다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 통화 이력 쌓기.

## 관련 문서

- [화면 캡처](/docs/page-view/inquiries-detail) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
