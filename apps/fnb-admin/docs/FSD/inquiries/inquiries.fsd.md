# Franchise Inquiries

> F&B Admin · `/inquiries` · id `inquiries`
> 원본: `apps/fnb-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Franchise Inquiries |
| 메뉴 위치 | 창업 > 문의 내역 |
| URL | `/inquiries` |
| 화면 목적 | 차리려는 사람이 남긴 것을 받아 밀리지 않게 본다. |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 창업 상담 신청 `/franchise/apply` — 여기로 쌓이는 유일한 입구다. · 창업안내 `/franchise` — 하루 안에 연락한다고 적어 둔 약속이 이 목록의 기한이다. |
| Figma 페이지 순번 | 30 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 목록 툴바 | 검색 · 상태 · 지역 | — |
| 표 | 한 줄이 문의 하나 — 관리 칸에 삭제가 없다 | — |
| 선택 줄 | 고른 줄 세기 | 줄을 골랐을 때 |
| 빈 상태 | 조건에 맞는 것이 없을 때 | 결과 0건 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 신청자·연락처·내용으로 검색 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 상태·지역으로 거르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 줄을 눌러 상세로 들어가기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 신청자·연락처·내용으로 검색 → 결과가 화면에 바로 반영된다.
- 상태·지역으로 거르기 → 결과가 화면에 바로 반영된다.
- 줄을 눌러 상세로 들어가기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 신청자·연락처·내용으로 검색 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 상태·지역으로 거르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 줄을 눌러 상세로 들어가기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 지우는 자리를 두지 않는다. 손님이 남긴 것이라 지우면 그 사람이 언제 무엇을 물었는지가 사라지고, 나중에 연락 못 받았다는 말에 댈 근거가 없어진다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 처리가 끝난 것은 상태로 닫는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 처음에 `접수` 만 걸어 두지 않는다 — 밀린 것만 보이면 상담 중인 건이 화면에서 사라져 아무도 안 보는 채로 남는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- `접수` 만 노란색이다 — 아직 아무도 손대지 않은 것이 그것뿐이다. 상담 중인 건까지 노랗게 두면 밀린 것과 진행 중인 것이 한 색으로 보인다.
- `보류` 는 회색이되 `완료` 와 갈린다 — 끝난 것이 아니라 멈춰 둔 것이라 언젠가 다시 열어야 한다.
- 밀린 것을 세는 일은 대시보드가 한다.
- 체크박스는 지울 수 없는 화면에도 선다 — 표마다 맨 왼쪽이 같은 자리여야 눈이 헤매지 않는다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 담당자 지정.

## 관련 문서

- [화면 캡처](/docs/page-view/inquiries) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
