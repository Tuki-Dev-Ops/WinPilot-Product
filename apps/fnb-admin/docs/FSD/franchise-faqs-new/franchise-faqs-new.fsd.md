# Franchise FAQ Create

> F&B Admin · `/franchise/faqs/new` · id `franchise-faqs-new`
> 원본: `apps/fnb-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Franchise FAQ Create |
| 메뉴 위치 | 창업 > (목록에서 진입) |
| URL | `/franchise/faqs/new` |
| 화면 목적 | 되풀이되는 창업 물음을 새로 적습니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 가맹점 개설문의 `/franchise/faq` — 공개를 켜야 선다. |
| Figma 페이지 순번 | 37 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 어느 갈래의 물음인가 | 글 번호 · 누가 묻나 · 분류 · 공개 | — |
| 묻고 답하기 | 질문과 답 | — |
| 저장 줄 | 등록 | — |
| 확인 창 | 분류와 질문을 한 줄로 다시 읽는다 | 등록을 눌렀을 때 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 분류 고르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 질문·답 넣기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 등록 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 글 번호 | 저장할 때 매겨진다 | 읽기 전용 | 선택 | — | 저장할 때 매겨집니다 |
| 누가 묻나 | 창업으로 고정된다 | 읽기 전용 | 선택 | — | 창업 |
| 분류 | 사이트 왼쪽 기둥 | 선택 | 선택 | 정해진 다섯 중 하나 | — |
| 질문 | 손님이 실제로 쓰는 말로 | 텍스트 | 필수 | — | — |
| 답 | 두세 줄 | 여러 줄 | 필수 | — | — |
| 공개 | 꺼진 채로 열린다 | 스위치 | 선택 | — | — |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 등록 | 입력을 확인하고 확인 창을 연다 | 토스트로 알린다 | 확인이 필요한 칸의 개수와 이름을 토스트에 적는다 |

## 5. 사용자 시나리오

### 정상

- 분류 고르기 → 결과가 화면에 바로 반영된다.
- 질문·답 넣기 → 결과가 화면에 바로 반영된다.
- 등록 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 분류 고르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 질문·답 넣기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 등록 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 공개를 끈 채로 열린다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 갈래는 `창업` 으로 화면이 정해서 넘긴다 — 여기서 만든 글이 다른 목록에 서면 안 된다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 글 번호를 미리 보여 주지 않는다. 저장할 때 매겨진다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 질문과 답은 공백만으로 둘 수 없다.
- 질문이 물음표로 끝나지 않으면 알린다 — 막지 않는다.

### 화면 정책

- 빈 값은 store 의 `blankFnbFaq(갈래)` 가 준다 — 갈래를 인자로 받는 까닭이 여기 있다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 문의 상세에서 곧바로 옮겨 적기.

## 관련 문서

- [화면 캡처](/docs/page-view/franchise-faqs-new) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
