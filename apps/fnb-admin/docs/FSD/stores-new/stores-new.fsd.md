# Store Create

> F&B Admin · `/stores/new` · id `stores-new`
> 원본: `apps/fnb-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Store Create |
| 메뉴 위치 | 등록 > (목록에서 진입) |
| URL | `/stores/new` |
| 화면 목적 | 새로 낼 매장을 미리 세워 둡니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 매장안내 `/stores` — 준비중도 서되 번호 자리가 비어 있다. |
| Figma 페이지 순번 | 22 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 매장 정보 | 코드 · 이름 · 지역 · 상태 · 주소 · 전화 · 개점 연월 · 영업시간 | — |
| 되는 것 | 포장 · 배달 · 주차 · 단체석 · 반려동물 | — |
| 저장 줄 | 등록 | — |
| 확인 창 | 이름 · 상태 · 주소를 한 줄로 다시 읽는다 | 등록을 눌렀을 때 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 매장명·지역·주소 넣기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 개점 연월 넣기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 되는 것 고르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 등록 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 코드 | 저장할 때 매겨진다 | 읽기 전용 | 선택 | — | 저장할 때 매겨집니다 |
| 매장명 | 매장 찾기에 서는 이름 | 텍스트 | 필수 | — | — |
| 지역 | 거르개가 이 값으로 나뉜다 | 선택 | 선택 | — | — |
| 상태 | 준비중으로 열린다 | 선택 | 선택 | 영업중 · 준비중 · 휴점 | — |
| 주소 | 지도앱에서 검색되는 형태 | 텍스트 | 필수 | — | — |
| 전화 | 준비중이면 비워 둔다 | 텍스트 | 선택 | 준비중이 아닐 때만 필수 | — |
| 개점 연월 | 사이트에 여는 달로 선다 | 텍스트 | 필수 | `YYYY-MM` | — |
| 영업시간 | 여는 시간과 닫는 시간 | 텍스트 | 필수 | — | — |
| 되는 것 | 정해진 다섯에서 고른다 | 고르개 | 선택 | — | — |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 등록 | 입력을 확인하고 확인 창을 연다 | 토스트로 알린다 | 확인이 필요한 칸의 개수와 이름을 토스트에 적는다 |

## 5. 사용자 시나리오

### 정상

- 매장명·지역·주소 넣기 → 결과가 화면에 바로 반영된다.
- 개점 연월 넣기 → 결과가 화면에 바로 반영된다.
- 되는 것 고르기 → 결과가 화면에 바로 반영된다.
- 등록 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 매장명·지역·주소 넣기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 개점 연월 넣기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 되는 것 고르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 등록 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 새 가맹점은 `준비중` 으로 열린다 — 등록하는 시점이 대개 공사 중이다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 준비중이라 번호가 비어 있어도 저장된다. 없는 번호로 전화가 가는 일이 없다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 코드를 미리 보여 주지 않는다. 저장할 때 매겨진다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 매장명 · 주소 · 영업시간은 공백만으로 둘 수 없다.
- 개점 연월은 `YYYY-MM` 모양이어야 한다.

### 화면 정책

- 빈 값은 store 의 `blankStore()` 가 준다 — 준비중으로 여는 판단도 거기 있다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 계약 정보와 잇기.

## 관련 문서

- [화면 캡처](/docs/page-view/stores-new) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
