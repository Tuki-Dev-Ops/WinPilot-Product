# Disclosures

> IR Client 템플릿 A · `/disclosures` · id `disclosures`
> 원본: `apps/ir-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Disclosures |
| 메뉴 위치 | 공시 > 공시 정보 |
| URL | `/disclosures` |
| 화면 목적 | 회사 이름으로 나간 공시만 최신순으로 세운다. |
| 기대 효과 | 어드민과 사이트가 같은 함수로 걸러, 올린 것과 보이는 것이 갈리지 않는다. |
| 어드민 연동 | IR > 공시 · IR > DART 연동 |
| Figma 페이지 순번 | 12 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | 공시 정보와 DART 에서도 확인할 수 있다는 안내 | — |
| IR 줄 | 아홉 화면 사이를 오가는 알약. 지금 것만 먹색이다 | — |
| 목록 | 공시일 · 갈래 딱지 · 제목 | — |
| 정정 딱지 | 고쳐 나간 공시임을 목록에서 알린다 | 상태가 정정일 때 |
| 빈 상태 | 나간 공시가 없을 때 | 목록 0건 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 공시 목록 훑기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 한 건을 눌러 상세 열기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 공시 목록 훑기 → 결과가 화면에 바로 반영된다.
- 한 건을 눌러 상세 열기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 공시 목록 훑기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 한 건을 눌러 상세 열기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| `작성 중` 과 `검토 요청` 은 목록에 오지 않는다 — 원고를 쓰는 사람과 회사 이름으로 내보내는 사람이 다르다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 공시가 하나도 없으면 등록된 공시가 없습니다 한 줄을 세운다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 사이트에 서는 것은 `공시됨` 과 `정정` 뿐이다(`docs/architecture/policy.md` §3).
- 차례는 공시일 내림차순이고 store 가 정한다. 화면에서 다시 정렬하지 않는다.
- 게시한 것은 되돌릴 수 없다. 나간 뒤에는 정정 공시로만 고친다.
- 거르개도 검색도 페이지 나눔도 두지 않는다. 지금 건수에서는 한 화면에 다 선다.
- 줄 전체가 링크다. 제목만 링크로 두면 날짜와 딱지 사이의 빈 자리를 눌러 본 사람이 고장으로 읽는다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 갈래 · 연도 거르개와 제목 검색.
- 목록에서 바로 여는 DART 링크. 지금은 상세에만 있다.
- 건수가 늘었을 때의 페이지 나눔.

## 관련 문서

- [화면 캡처](/docs/page-view/disclosures) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
