# Cloud MES

> IR Client 템플릿 A · `/solutions/mes` · id `solutions-mes`
> 원본: `apps/ir-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Cloud MES |
| 메뉴 위치 | PRODUCT > Cloud MES |
| URL | `/solutions/mes` |
| 화면 목적 | 설비 신호를 하나의 규격으로 모으는 제품이 어떤 문제를 푸는지 보여 드립니다. |
| 배경 | 제품 소개 화면의 카드 한 장이 전부였습니다. 카드에는 한 줄밖에 담기지 않아 무엇이 불편해서 이 제품을 쓰는지가 어디에도 남지 않았고, 검토하시는 분은 그 답을 영업 담당자에게 따로 물어야 했습니다. |
| 기대 효과 | 네 제품이 같은 순서로 서기 때문에, 나란히 놓고 견주어 보실 수 있습니다. |
| 어드민 연동 | 제품 > 목록 · 설정 > 공급자 정보 |
| Figma 페이지 순번 | 8 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | 이름과 한 줄 | — |
| 문제 · 기능 · 구성 · 성과 · 업종 · 절차 · 문의 | 여섯 상세가 같은 차례로 선다 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 문제 · 기능 · 구성 · 성과 · 업종 · 절차 읽기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 맨 아래 문의 칸에서 문의하기로 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 문제 · 기능 · 구성 · 성과 · 업종 · 절차 읽기 → 결과가 화면에 바로 반영된다.
- 맨 아래 문의 칸에서 문의하기로 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 문제 · 기능 · 구성 · 성과 · 업종 · 절차 읽기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 맨 아래 문의 칸에서 문의하기로 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 값을 찾지 못하면 404 다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 내려 둔 제품도 주소로는 열린다 — 아직 팔지 않는 제품의 소개를 미리 만들어 두고 길만 감추는 일이 흔하다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 주소는 `/solutions/mes` 인데 갈래는 PRODUCT 다. 갈래를 옮길 때 주소까지 옮기면 밖에 나간 링크가 끊긴다.
- 기능은 넷으로 고정한다. 다섯째부터는 앞의 넷을 다시 말하는 줄이 되기 쉽다.
- 구성은 아래에서 위로 쌓이는 층이다. 번호가 그 차례를 말한다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 설비 연동 목록 · 프로토콜 표.
- 체험 계정.

## 관련 문서

- [화면 캡처](/docs/page-view/solutions-mes) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
