# Meetings

> IR Client 템플릿 A · `/meetings` · id `meetings`
> 원본: `apps/ir-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Meetings |
| 메뉴 위치 | 주주 > 주주총회 |
| URL | `/meetings` |
| 화면 목적 | 언제 어디서 무엇을 의결하는지를 안건까지 전부 보인다. |
| 기대 효과 | 안건을 줄이지 않아, 무엇을 의결하는지 모른 채 참석 여부를 정하는 일이 없다. |
| 어드민 연동 | IR > 주주총회 |
| Figma 페이지 순번 | 30 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | 주주총회와 소집 공고 · 안건이라는 한 줄 | — |
| IR 줄 | 아홉 화면 사이를 오가는 알약 | — |
| 총회 카드 | 상태 딱지 · 제목 · 개최일 · 장소 · 안건 목록 | — |
| 전자투표 안내로 | 의결권을 행사할 길 | 그 총회가 전자투표를 받을 때 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 총회 카드 읽기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 안건 전부 읽기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 전자투표 안내로 넘어가기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 총회 카드 읽기 → 결과가 화면에 바로 반영된다.
- 안건 전부 읽기 → 결과가 화면에 바로 반영된다.
- 전자투표 안내로 넘어가기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 총회 카드 읽기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 안건 전부 읽기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 전자투표 안내로 넘어가기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 전자투표를 받지 않는 총회에는 안내 링크를 그리지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 예정된 총회만 색 있는 딱지를 단다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 안건을 전부 적는다. 목록에서 줄이면 주주가 무엇을 의결하는지 모른 채 참석 여부를 정하게 되는데, 이 화면에서 줄여도 되는 값이 아니다.
- 표가 아니라 카드다. 안건이 가변 길이 목록이라 표 칸에 들어가지 않는다.
- 종료된 총회도 그대로 둔다. 지난 안건을 확인하러 오는 사람이 있고, 지우면 그 사람은 갈 곳이 없다.
- 차례는 store 가 든 순서 그대로다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 소집 공고 원문과 위임장 서식.
- 결의 결과.
- 예정 · 종료 나누기.

## 관련 문서

- [화면 캡처](/docs/page-view/meetings) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
