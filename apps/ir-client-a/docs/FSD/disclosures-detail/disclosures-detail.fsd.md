# Disclosure Detail

> IR Client 템플릿 A · `/disclosures/[disclosureId]` · id `disclosures-detail`
> 원본: `apps/ir-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Disclosure Detail |
| 메뉴 위치 | 공시 > 공시 상세 |
| URL | `/disclosures/[disclosureId]` · 예 `/disclosures/D-2026-014` |
| 화면 목적 | 나간 공시 한 건의 본문과 정정 사실, 원문 링크를 준다. |
| 기대 효과 | 아직 안 나간 원고가 번호를 짐작해 들어온 사람에게 보이지 않는다. |
| 어드민 연동 | IR > 공시 · IR > DART 연동 |
| Figma 페이지 순번 | 13 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 돌아가는 길 | 제목 위에 공시 정보로 | 주소로 바로 들어온 사람에게도 돌아갈 곳이 필요하다 |
| 제목 | 공시 제목과 공시일 · 갈래 | — |
| 정정 안내 | 어느 공시를 정정한 것인지 | 정정 대상 번호가 있을 때 |
| 본문 | 어드민 편집기가 만든 글 | — |
| DART 원문 | 원문으로 나가는 길 | 원문 주소가 있을 때 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 본문 읽기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| DART 원문 열기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 목록으로 돌아가기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 본문 읽기 → 결과가 화면에 바로 반영된다.
- DART 원문 열기 → 결과가 화면에 바로 반영된다.
- 목록으로 돌아가기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 본문 읽기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** DART 원문 열기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 목록으로 돌아가기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 없는 id 는 404 다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| `작성 중` · `검토 요청` 인 공시도 404 다. 나가지 않은 공시는 **주소로도 열리지 않는다** — 목록에서 거르는 것만으로는 부족하다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 없는 공시와 아직 안 나간 공시가 같은 404 다. 둘을 갈라 보이면 그 번호의 원고가 있다는 사실이 새어 나간다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 미리 만들어 두는 경로는 나간 공시만이다. 원고까지 만들어 두면 그 화면이 검색에 걸린다.
- 상세에는 IR 줄을 두지 않는다. 목록의 자식이라 돌아가는 길 하나만 둔다.
- 정정은 목록의 딱지와 상세의 안내 두 곳에 남는다. 무엇을 고친 것인지는 여기가 적는다.
- 본문은 어드민이 만든 값이라 밖에서 들어온 입력이 아니다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 정정 대상 공시로 가는 링크. 지금은 번호를 글자로만 적는다.
- 이전 · 다음 공시.
- 제목을 공시명으로 바꾸는 문서 제목.

## 관련 문서

- [화면 캡처](/docs/page-view/disclosures-detail) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
