# IR Library

> IR Client 템플릿 A · `/library` · id `library`
> 원본: `apps/ir-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | IR Library |
| 메뉴 위치 | 자료 · 알림 > IR 자료실 |
| URL | `/library` |
| 화면 목적 | 실적발표 · 사업보고서 같은 자료를 크기와 함께 내려받게 한다. |
| 기대 효과 | 크기가 적혀 있어 모바일에서 큰 파일을 모르고 누르는 일이 없다. |
| 어드민 연동 | IR > IR 자료실 |
| Figma 페이지 순번 | 40 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | IR 자료실과 무엇을 내려받을 수 있는지 | — |
| IR 줄 | 아홉 화면 사이를 오가는 알약 | — |
| 표 | 자료 · 구분 · 올린 날 · 크기 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 자료 목록 훑기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 자료 눌러 내려받기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 자료 목록 훑기 → 결과가 화면에 바로 반영된다.
- 자료 눌러 내려받기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 자료 목록 훑기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 자료 눌러 내려받기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 자료가 하나도 없으면 등록된 자료가 없습니다 한 줄을 세운다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 크기를 반드시 적는다. 모바일에서 8MB 를 모르고 누르는 것과 알고 누르는 것은 다르다(`docs/architecture/policy.md` §3).
- 갈래로 화면을 나누지 않고 표의 한 칸으로만 적는다. 자료 수가 적어 나누면 고르는 자리만 늘어난다.
- 차례는 store 가 든 순서 그대로다.
- 제목만 링크다. 줄 전체를 링크로 두면 크기를 읽으려다 파일이 받아진다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 실제 파일. 지금은 주소가 비어 있어 눌러도 받아지는 것이 없다.
- 갈래 거르개 · 연도 묶음 · 검색.
- 파일이 아직 붙지 않은 자료를 표에서 알리기.

## 관련 문서

- [화면 캡처](/docs/page-view/library) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
