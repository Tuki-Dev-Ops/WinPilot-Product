# Governance

> IR Client 템플릿 A · `/governance` · id `governance`
> 원본: `apps/ir-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Governance |
| 메뉴 위치 | 주주 > 지배구조 |
| URL | `/governance` |
| 화면 목적 | 누가 회사를 맡고 있고 누가 갖고 있는지를 두 표로 보인다. |
| 기대 효과 | 사외 여부가 이름 옆에 서서, 지배구조에서 먼저 읽는 값이 먼저 보인다. |
| 어드민 연동 | IR > 지배구조 |
| Figma 페이지 순번 | 32 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | 지배구조와 이사회 구성 · 주주 현황이라는 한 줄 | — |
| IR 줄 | 아홉 화면 사이를 오가는 알약 | — |
| 이사회 표 | 이름 · 직위 · 구분 · 주요 경력 | — |
| 주주 현황 표 | 구분 · 주주 · 지분율 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 이사회 구성 읽기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 주주 현황과 지분율 읽기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 이사회 구성 읽기 → 결과가 화면에 바로 반영된다.
- 주주 현황과 지분율 읽기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 이사회 구성 읽기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 주주 현황과 지분율 읽기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 임원이 없으면 등록된 임원이 없습니다 한 줄을 세운다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 주주가 없으면 등록된 주주 정보가 없습니다 한 줄을 세운다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 사외인지 사내인지를 이름 · 직위 바로 옆에 둔다 — 지배구조에서 가장 먼저 읽는 값이다.
- 두 표를 한 화면에 둔다. 맡은 사람과 가진 사람을 같이 봐야 지배구조가 된다.
- 차례는 store 가 든 순서 그대로다. 서열과 지분 순서가 그 차례에 이미 있다.
- 지분 합이 맞는지 재는 것은 어드민이 한다. 두 곳에서 재면 어느 쪽이 맞는지 화면으로는 알 수 없다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 위원회 구성과 임기 · 선임일.
- 지분율 합계 줄과 기준일 표기.
- 최대주주 변동 이력.

## 관련 문서

- [화면 캡처](/docs/page-view/governance) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
