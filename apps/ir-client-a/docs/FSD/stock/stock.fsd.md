# Stock

> IR Client 템플릿 A · `/stock` · id `stock`
> 원본: `apps/ir-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Stock |
| 메뉴 위치 | 숫자 > 주가 정보 |
| URL | `/stock` |
| 화면 목적 | 현재가와 등락을 보이되 지연 시세임을 놓칠 수 없게 한다. |
| 기대 효과 | 실시간으로 읽고 그 차이로 판단한 뒤에야 아는 일이 없어진다. |
| 어드민 연동 | IR > 주가 연동 |
| Figma 페이지 순번 | 21 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | 주가 정보와 시장 · 종목코드 · 지연 시세라는 고지 | — |
| IR 줄 | 아홉 화면 사이를 오가는 알약 | — |
| 시세 상자 | 큰 현재가와 등락, 그 옆에 기준 시각과 지연 표시 | — |
| 표 | 거래량 · 시가총액 · 52주 최고 · 52주 최저 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 현재가 · 등락 확인 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 거래량 · 시가총액 · 52주 최고 · 52주 최저 확인 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 기준 시각 확인 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 현재가 · 등락 확인 → 결과가 화면에 바로 반영된다.
- 거래량 · 시가총액 · 52주 최고 · 52주 최저 확인 → 결과가 화면에 바로 반영된다.
- 기준 시각 확인 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 현재가 · 등락 확인 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 거래량 · 시가총액 · 52주 최고 · 52주 최저 확인 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 기준 시각 확인 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 값을 불러오지 못하면 주가 정보를 불러오지 못했습니다 한 줄을 세운다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 지연 시세임을 **늘** 적는다. 제목 줄과 숫자 옆 두 곳이고, 기준 시각은 숫자 바로 옆이다(`docs/architecture/policy.md` §3).
- 몇 분 지연인지는 적지 않는다. 시세를 어디서 받아 오는지를 정하는 것은 어드민이고 그 값이 아직 사이트로 이어지지 않아, 숫자를 적으면 지키지 못할 말이 된다.
- 주가를 손으로 고치는 칸을 두지 않는다.
- 등락 색은 상승이 빨강이다. 국내 관행이라 그렇게 두되 화살표를 함께 세운다.
- 시가총액만 칸 안에 단위를 적는다. 다른 값과 자릿수가 크게 달라 단위를 빼면 잘못 읽힌다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 기간 차트와 52주 범위 안에서 현재가의 자리.
- 시가 · 고가 · 저가 · 전일 종가.
- 어드민의 시세 출처 · 지연 설정과 실제로 잇기.

## 관련 문서

- [화면 캡처](/docs/page-view/stock) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
