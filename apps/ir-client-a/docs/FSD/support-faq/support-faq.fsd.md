# FAQ

> IR Client 템플릿 A · `/support/faq` · id `support-faq`
> 원본: `apps/ir-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | FAQ |
| 메뉴 위치 | CS CENTER > FAQ |
| URL | `/support/faq` |
| 화면 목적 | 자주 받는 물음을 갈래와 검색으로 찾아 그 자리에서 펴 읽게 한다. |
| 기대 효과 | 세 묶음을 세로로 지나지 않아, 기술 문제로 온 사람이 도입 이야기를 먼저 읽지 않는다. |
| 어드민 연동 | 콘텐츠 > FAQ |
| Figma 페이지 순번 | 63 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | FAQ 와 갈래를 고르거나 검색해 보라는 한 줄 | — |
| 왼쪽 갈래 | 전체 · 도입 · 기술 · 지원. 옆에 몇 건인지 함께 | — |
| 검색과 건수 | 거른 결과가 몇 건인지 | — |
| 접히는 목록 | 갈래 · 물음 · 화살표. 눌러서 답을 편다 | — |
| 빈 상태 | 조건에 맞는 물음이 없을 때 | 결과 0건 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 왼쪽에서 갈래 고르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 물음과 답으로 검색 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 물음 펴 보기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 갈래 | 도입 · 기술 · 지원 중 무엇을 볼지 | 선택 | 선택 | 코드가 정한 셋. 없는 갈래도 0 으로 세운다 | 기술 |
| 검색어 | 물음과 답을 함께 훑는다 | 텍스트 | 선택 | 앞뒤 공백 제거 · 대소문자 가리지 않음 · 부분 일치 | 견적 |

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 왼쪽에서 갈래 고르기 → 결과가 화면에 바로 반영된다.
- 물음과 답으로 검색 → 결과가 화면에 바로 반영된다.
- 물음 펴 보기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 왼쪽에서 갈래 고르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 물음과 답으로 검색 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 물음 펴 보기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 숨긴 물음은 오지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 한 번에 하나만 열린다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 조건에 맞는 것이 없으면 조건에 맞는 물음이 없다고 적고 문의를 권한다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 검색이 답까지 훑는다. 물음의 말과 찾는 사람의 말이 다르다 — 견적을 치는 사람의 물음은 도입 비용은 어떻게 되나요로 적혀 있고, 그 말은 답 안에 있다.
- 공지사항과 줄 모양까지 같게 맞췄다. 브라우저가 주는 접기 요소를 여기만 써서 줄 높이와 여백이 미묘하게 달랐는데, 화면을 옮길 때 눈에 걸리는 것이 그 차이다.
- 잃은 것을 대신한다 — 펼침 상태를 낭독기가 읽고, 키보드는 원래 단추라 그대로 된다. 접힌 답이 브라우저 찾기에 걸리지 않는 것만 남는데, 답까지 훑는 검색창이 그 자리를 메운다.
- 한 번에 하나만 편다. 여럿을 펼쳐 두면 화면이 길어져 다음 물음이 어디 있는지 다시 찾아야 한다.
- 맨 위 하나는 펴 둔 채로 들어온다.
- 갈래는 코드가 정한 것을 쓴다. 그 기간에 한 건도 없는 갈래가 목록에서 사라지면 그 갈래가 아예 없는 것으로 읽힌다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 빈 상태에서 문의로 바로 가는 링크. 지금은 글자로만 권한다.
- 도움이 되었나요 응답.
- 물음 하나를 가리키는 주소.

## 관련 문서

- [화면 캡처](/docs/page-view/support-faq) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
