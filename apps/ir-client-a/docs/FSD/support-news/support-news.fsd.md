# Support News

> IR Client 템플릿 A · `/support/news` · id `support-news`
> 원본: `apps/ir-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Support News |
| 메뉴 위치 | CS CENTER > 뉴스 |
| URL | `/support/news` |
| 화면 목적 | 방송과 행사, 제품 소개로 남은 자료를 찾으러 오신 분께 전부 펼쳐 드립니다. |
| 배경 | 영상과 보도자료가 홈 맨 아래 한 줄에만 있었습니다. 그 줄은 다섯 장만 보여 주고 옆으로 밀어야 해서, 지난 소식을 찾으러 오신 분은 끝까지 밀어 보고는 없다고 판단하셨습니다. |
| 기대 효과 | 홈에서 옆으로 밀어 보시다 관심이 생긴 분이 이어서 가실 곳이 생겼습니다. |
| 어드민 연동 | 콘텐츠 > 뉴스 |
| Figma 페이지 순번 | 62 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | 뉴스와 방송 · 행사 · 제품 소개로 남은 것들이라는 한 줄 | — |
| 왼쪽 갈래 | 전체와 실제로 쓰이는 갈래. 옆에 몇 건인지 함께 | — |
| 검색과 건수 | 거른 결과가 몇 건인지 | — |
| 격자 | 무늬 · 재생 표시 · 갈래 · 제목 | — |
| 빈 상태 | 조건에 맞는 뉴스가 없을 때 | 결과 0건 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 왼쪽에서 갈래 고르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 제목과 갈래로 검색 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 격자 훑기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 갈래 | 어디에 실린 것을 볼지 | 선택 | 선택 | 값에서 뽑는다. 나온 차례를 지킨다 | 제품 소개 |
| 검색어 | 제목과 갈래를 함께 훑는다 | 텍스트 | 선택 | 앞뒤 공백 제거 · 대소문자 가리지 않음 · 부분 일치 | 세미나 |

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 왼쪽에서 갈래 고르기 → 결과가 화면에 바로 반영된다.
- 제목과 갈래로 검색 → 결과가 화면에 바로 반영된다.
- 격자 훑기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 왼쪽에서 갈래 고르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 제목과 갈래로 검색 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 격자 훑기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 숨긴 영상은 오지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 조건에 맞는 것이 없으면 조건에 맞는 뉴스가 없습니다 한 줄을 세운다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 카드는 누를 수 없다 — 영상 파일이 아직 없어, 누를 수 있게 보이면 눌러 본 사람이 고장으로 읽는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 셋 중 여기만 결과가 격자다. 영상은 제목보다 그림으로 고른다.
- 갈래를 값에서 뽑는다. 적어 두면 어드민에서 새 갈래를 쓰는 날 그 영상만 어느 갈래에도 안 걸린다 — 공지 · FAQ 와 다른 점인데, 저쪽은 갈래가 코드에 정해져 있어 없는 갈래도 0 으로 세워 두는 편이 낫다.
- 갈래 차례는 나온 순서다. 가나다로 섞으면 최근 갈래가 뒤로 간다.
- 홈의 줄과 같은 무늬를 쓴다. 여기서만 다르게 그리면 홈에서 본 것과 같은 영상인지 알 수 없다.
- 홈에서는 다섯 장 남짓만 흘리고 여기서는 전부 펼친다. 찾으러 온 사람이 보는 자리이기 때문이다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 영상 파일과 재생. 값에 주소 자리가 아직 없다.
- 올린 날. 지금은 갈래와 제목만 있다.
- 뉴스 상세.

## 관련 문서

- [화면 캡처](/docs/page-view/support-news) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
