# About

> IR Client 템플릿 A · `/about` · id `about`
> 원본: `apps/ir-client-a/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | About |
| 메뉴 위치 | ABOUT > 회사 소개 |
| URL | `/about` |
| 화면 목적 | 확인하러 온 사람에게 회사의 신원을 한 화면에서 준다. |
| 기대 효과 | 표만 있던 화면에 숫자 넉 줄이 붙어, 처음 온 사람도 읽을 것이 생긴다. |
| 어드민 연동 | 회사 > 소개 · 회사 > 연혁 · 회사 > 특허 및 인증 · 설정 > 공급자 정보 |
| Figma 페이지 순번 | 2 |

이 화면이 읽는 값의 출처다. 값을 템플릿에 박아 두면 고칠 때마다 배포해야 한다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 제목 | 회사 소개와 한 줄 소개 | — |
| 숫자 넉 줄 | 설립 연도 · 상장 시장 · 연혁 수 · 특허와 인증 수 | — |
| 회사 정보 표 | 회사명 · 대표이사 · 사업자등록번호 · 설립일 · 상장일 · 종목코드 · 본사 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 설립 · 상장 · 연혁 수 · 특허와 인증 수 확인 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 회사 정보 표 읽기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 설립 · 상장 · 연혁 수 · 특허와 인증 수 확인 → 결과가 화면에 바로 반영된다.
- 회사 정보 표 읽기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 설립 · 상장 · 연혁 수 · 특허와 인증 수 확인 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 회사 정보 표 읽기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 긴 소개 글을 두지 않는다. 홈이 이미 말했고, 같은 말이 두 곳에 있으면 한쪽만 고쳐진다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 숨긴 연혁과 비공개 특허·인증은 숫자에 들지 않는다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 숫자 넉 줄은 지어내지 않고 이미 있는 값을 센다. 연혁 수와 특허·인증 수는 거른 뒤의 것이라 목록 화면과 같은 답이 된다.
- 이름 아래에 값을 둔다. 훑는 눈은 작은 글씨를 먼저 지나 큰 글씨에서 멈추고, 차례가 반대면 값을 보고 나서 그것이 무엇인지 되짚어 올라가야 한다.
- 설립 연도만 잘라 쓴다. 날짜를 그대로 두면 넉 줄 중 이 칸만 길어져 줄이 어긋난다.
- 끝에 두었던 바로가기 두 장(연혁 · 특허 및 인증)을 뺐다. 헤더의 ABOUT 펼침에 이미 서 있고, 화면이 끝났다는 신호가 있어야 할 자리에 링크 둘이 서면 아래가 더 있는 것처럼 읽힌다.
- 표 차례를 고정한다 — 회사명부터 본사까지. 확인하러 온 사람은 늘 같은 자리를 본다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 대표 인사말 · 사옥 사진.
- 영문 화면. 어드민에 국문 · 영문 짝은 있고 사이트가 아직 읽지 않는다.

## 관련 문서

- [화면 캡처](/docs/page-view/about) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
