/**
 * 기준일 — **서버가 없으므로 화면 밖에서 정한다.**
 *
 * 화면이 `new Date()` 를 읽으면 서버가 그린 값과 브라우저가 그린 값이 갈려 하이드레이션이
 * 깨진다. 게다가 화면마다 읽으면 자정을 넘기는 순간 **한 화면은 어제, 옆 화면은 오늘**을
 * 기준으로 D-n 을 센다.
 *
 * 서버가 붙으면 이 값은 요청이 실어 보내는 값으로 바뀐다. 그때 고칠 자리가 한 곳이도록 둔다.
 */
export const TODAY = '2026-08-06';
