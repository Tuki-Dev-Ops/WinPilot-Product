# Terms

> IR Admin · `/settings/terms` · id `settings-terms`
> 원본: `apps/ir-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Terms |
| 메뉴 위치 | 설정 > 서비스 이용약관 |
| URL | `/settings/terms` |
| 화면 목적 | 고객 사이트 이용약관 본문을 관리하고 게시 여부를 설정하는 페이지입니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 고객 사이트 하단 서비스 이용약관 링크가 연결되는 화면 |
| Figma 페이지 순번 | 92 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 본문 입력 영역 | 검토를 완료한 약관 본문 | — |
| 게시 설정 영역 | 게시 또는 초안 저장 | — |
| 저장 확인 모달 | 게시 여부를 구분하여 표시 | 저장 버튼을 선택했을 때 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 처리 | 결과 |
|---|---|---|---|
| 약관 본문 등록 기능 제공 | 약관 본문 입력 | 입력값을 검증한 뒤 신규 항목으로 저장한다 | 저장 결과를 안내하고 목록 화면으로 이동한다 |
| 초안 저장 기능 제공 | 초안 저장 | 게시 여부에 따른 확인 모달을 표시합니다 | 게시 여부를 구분하여 안내합니다 / 본문 미입력으로 게시할 수 없음을 안내합니다 |
| 약관 게시 기능 제공 | 약관 게시 | 해당 항목의 상태값을 변경한다 | 변경된 상태를 목록과 고객 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 서비스 이용약관 | 게시 시 효력을 주장하는 문서가 됩니다 | 여러 줄 | 필수 | 게시하려면 미입력 상태일 수 없습니다 | — |
| 게시 | 고객 사이트 게시 또는 초안 유지 | 스위치 | 선택 | — | — |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 저장 | 게시 여부에 따른 확인 모달을 표시합니다 | 게시 여부를 구분하여 안내합니다 | 본문 미입력으로 게시할 수 없음을 안내합니다 |

## 5. 사용자 시나리오

### 정상

- 약관 본문 입력 → 결과가 화면에 바로 반영된다.
- 초안 저장 → 결과가 화면에 바로 반영된다.
- 약관 게시 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 약관 본문 입력 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 초안 저장 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 약관 게시 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 게시 여부를 저장과 분리하여 관리합니다. 검토 대기 중에는 고객 사이트에 준비 중 안내만 표시되어야 합니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 본문이 미입력이면 게시할 수 없습니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 게시와 초안 저장의 확인 모달 문구를 구분합니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 샘플 문구를 사전 입력하지 않습니다. 이행 불가능한 조항이 포함되면 문서 전체의 신뢰도가 저하됩니다.
- 검토 전 초안을 게시하면 해당 시점부터 효력을 주장하는 문서가 됩니다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 개정 이력 관리.

## 관련 문서

- [화면 캡처](/docs/page-view/settings-terms) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
