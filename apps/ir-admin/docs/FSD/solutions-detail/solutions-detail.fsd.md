# Problem & Approach Detail

> IR Admin · `/solutions/[solutionId]` · id `solutions-detail`
> 원본: `apps/ir-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Problem & Approach Detail |
| 메뉴 위치 | 메뉴 밖 |
| URL | `/solutions/[solutionId]` · 예 `/solutions/erp` |
| 화면 목적 | 해결 과제와 해결 방식을 수정하는 상세 페이지입니다. 제품 상세와 동일한 폼을 사용합니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 고객 사이트 제품 상세 `/solutions/{id}` · 메인 페이지 제품 카드 |
| Figma 페이지 순번 | 41 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 소개 문구 입력 영역 | 카드 · 메뉴 · 상세 페이지가 공통으로 조회하는 값 | — |
| 구성 항목 조회 영역 | 주요 기능 · 시스템 구성 · 적용 업종 · 진행 절차. 조회 전용 | — |
| 저장 확인 모달 | 고객 사이트 반영 내용을 요약 표시 | 저장 버튼을 선택했을 때 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 처리 | 결과 |
|---|---|---|---|
| 제품명 및 소개 문구 수정 기능 제공 | 제품명 및 소개 문구 수정 | 입력값을 검증한 뒤 기존 항목을 갱신한다 | 저장 결과를 안내한다 |
| 해결 과제 및 해결 방식 수정 기능 제공 | 해결 과제 및 해결 방식 수정 | 입력값을 검증한 뒤 기존 항목을 갱신한다 | 저장 결과를 안내한다 |
| 노출 상태 변경 기능 제공 | 노출 상태 변경 | 해당 항목의 상태값을 변경한다 | 변경된 상태를 목록과 고객 화면에 반영한다 |
| 저장 기능 제공 | 저장 | 입력값을 검증한 뒤 기존 항목을 갱신한다 | 저장 결과를 안내한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 제품명 및 소개 문구 수정 → 결과가 화면에 바로 반영된다.
- 해결 과제 및 해결 방식 수정 → 결과가 화면에 바로 반영된다.
- 노출 상태 변경 → 결과가 화면에 바로 반영된다.
- 저장 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 제품명 및 소개 문구 수정 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 해결 과제 및 해결 방식 수정 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 노출 상태 변경 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 저장 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 구성 항목(주요 기능 · 시스템 구성 · 적용 업종 · 진행 절차)은 조회 전용입니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 제품 코드와 상세 페이지 URL 은 수정할 수 없습니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 저장 시 확인 모달을 표시합니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 제품 · 서비스 상세와 동일한 폼 컴포넌트를 사용합니다. 3개 메뉴가 동일한 데이터 구조(`Offering`)를 조회하므로 화면을 분리하면 항목 불일치가 발생합니다. 이전 목록 경로만 다르게 전달합니다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 구성 항목 수정 기능.

## 관련 문서

- [화면 캡처](/docs/page-view/solutions-detail) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
