# Page Visits

> IR Admin · `/statistics/pages` · id `statistics-pages`
> 원본: `apps/ir-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Page Visits |
| 메뉴 위치 | 통계 > 많이 방문한 페이지 |
| URL | `/statistics/pages` |
| 화면 목적 | 어느 화면이 실제로 읽히는지 본다. |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 없음 — 운영자만 보는 화면이다. |
| Figma 페이지 순번 | 72 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 화면별 표 | 주소 · 이름 · 방문 수 · 머문 시간 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 화면별 방문 수 읽기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 머문 시간과 함께 견주기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 화면별 방문 수 읽기 → 결과가 화면에 바로 반영된다.
- 머문 시간과 함께 견주기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 화면별 방문 수 읽기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 머문 시간과 함께 견주기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 읽기만 하는 화면이다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 방문 수와 머문 시간을 함께 본다. 방문만 보면 홈이 늘 1등이라 아무것도 알 수 없고, 머문 시간이 짧은데 방문이 많은 화면이 들어왔다 바로 나간 자리다 — 그것이 고쳐야 할 화면이다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 들어온 길(검색어 · 유입처)까지 보기.

## 관련 문서

- [화면 캡처](/docs/page-view/statistics-pages) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
