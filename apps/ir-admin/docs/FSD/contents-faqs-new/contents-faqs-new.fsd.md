# FAQ Create

> IR Admin · `/contents/faqs/new` · id `contents-faqs-new`
> 원본: `apps/ir-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | FAQ Create |
| 메뉴 위치 | 콘텐츠 > (목록에서 진입) |
| URL | `/contents/faqs/new` |
| 화면 목적 | 되풀이되는 물음 하나를 옮겨 적어 문의를 줄입니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 사이트 CS CENTER > FAQ — 노출을 켜야 선다. |
| Figma 페이지 순번 | 27 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 물음과 답 | 코드 · 갈래 · 물음 · 답 · 노출 | — |
| 비슷한 물음 | 같은 갈래의 다른 물음 — 자기 자신은 뺀다 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 갈래 고르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 물음 적기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 답 적기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 노출 정하기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 등록 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| FAQ 코드 | FAQ 를 가리키는 번호 | 읽기 전용 | 선택 | 저장할 때 매겨진다 | F-02 |
| 갈래 | 사이트 FAQ 화면 왼쪽 줄이 된다 | 선택 | 필수 | 도입 · 기술 · 지원 | — |
| 물음 | 사람이 실제로 묻는 말 그대로 | 텍스트 | 필수 | — | 도입까지 얼마나 걸리나요? |
| 답 | 되묻지 않아도 되게 적는다 | 여러 줄 | 필수 | 글상자를 열두 줄로 연다 | — |
| 사이트에 노출 | 끄면 사이트에서 사라진다 | 스위치 | 선택 | — | — |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 등록 | 입력을 확인하고 확인 창을 연다 | 토스트로 알린다 | 확인이 필요한 칸을 토스트에 적는다 |

## 5. 사용자 시나리오

### 정상

- 갈래 고르기 → 결과가 화면에 바로 반영된다.
- 물음 적기 → 결과가 화면에 바로 반영된다.
- 답 적기 → 결과가 화면에 바로 반영된다.
- 노출 정하기 → 결과가 화면에 바로 반영된다.
- 등록 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 갈래 고르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 물음 적기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 답 적기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 노출 정하기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 등록 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 적는 동안 같은 갈래의 다른 물음을 옆에 세운다. 막지는 않는다 — 정말 다른 물음인지는 사람만 안다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 코드는 저장할 때 매겨진다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 갈래 · 물음 · 답은 공백만으로 둘 수 없다.

### 화면 정책

- 답 상자를 열두 줄로 연다. 짧게 열어 두면 짧게 적게 되고, 짧은 답은 다시 문의로 돌아온다 — FAQ 가 하는 일이 그 되돌아옴을 줄이는 것이다.
- 수정 화면에서는 자기 자신을 이웃 목록에서 뺀다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 비슷한 물음을 눌러 그리로 넘어가기.

## 관련 문서

- [화면 캡처](/docs/page-view/contents-faqs-new) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
