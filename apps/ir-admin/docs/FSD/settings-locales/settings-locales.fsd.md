# Locales

> IR Admin · `/settings/locales` · id `settings-locales`
> 원본: `apps/ir-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Locales |
| 메뉴 위치 | 설정 > 국문 · 영문 |
| URL | `/settings/locales` |
| 화면 목적 | 사이트 문구를 국문 · 영문 **짝으로** 관리한다. |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 사이트의 메뉴 이름과 화면 제목 — 두 언어가 같은 자리에서 나간다. |
| Figma 페이지 순번 | 94 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

![Locales 데스크톱](/page-view/settings-locales/settings-locales-desktop.jpg)

세 너비(데스크톱·태블릿·모바일)와 예외 화면은 `apps/ir-admin/docs/page-view/settings-locales/` 에서 본다.

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 문구 목록 | 한 줄에 국문과 영문을 나란히 — 영문이 없으면 `아직 없음` | — |
| 고치는 창 | 국문과 영문 두 칸 | — |
| 확인 창 | 어느 자리를 어떤 말로 바꾸는지 다시 읽는다 | 저장을 눌렀을 때 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 짝 목록에서 아직 안 된 것 찾기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 줄을 눌러 창에서 고치기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 저장 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 국문 | 사이트의 원문 | 텍스트 | 필수 | — | 공시 정보 |
| 영문 | 비워 두면 국문으로 대신 보여 준다 | 텍스트 | 선택 | — | Disclosures |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 저장 | 국문이 찼는지 보고 확인 창을 연다 | 토스트에 영문이 아직 없는지까지 함께 적는다 | 저장하지 못했다고 토스트에 적는다 |

## 5. 사용자 시나리오

### 정상

- 짝 목록에서 아직 안 된 것 찾기 → 결과가 화면에 바로 반영된다.
- 줄을 눌러 창에서 고치기 → 결과가 화면에 바로 반영된다.
- 저장 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 짝 목록에서 아직 안 된 것 찾기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 줄을 눌러 창에서 고치기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 저장 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 국문은 지울 수 없다 — 영문이 비면 원문으로 대신하지만 국문이 비면 사이트의 그 자리가 통째로 사라진다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 틀린 값(국문 비어 있음)은 확인 창까지 가지 않는다 — 물어봐야 할 것이 없다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 저장 앞에 확인 창이 선다. 한 자리를 바꾸면 그 말이 화면 여러 곳에서 동시에 바뀌고, 무엇이 바뀌었는지는 사이트를 열어 봐야 안다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 국문이 비면 저장을 막는다.

### 화면 정책

- 언어마다 화면을 나누지 않는다. 나누면 무엇이 아직 안 됐는지를 사람이 눈으로 견줘야 하고, 영문만 빠진 자리는 해외 투자자에게 **그 자리가 없는 것**과 같다.
- 확인 창은 언제나 다른 창 위에서 열린다 — 그 층은 확인 창이 스스로 갖는다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 영문이 빠진 자리를 한 번에 훑는 거르개.

## 관련 문서

- [화면 캡처](/docs/page-view/settings-locales) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
