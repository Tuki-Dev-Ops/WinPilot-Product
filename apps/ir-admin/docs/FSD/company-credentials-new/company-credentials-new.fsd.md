# Credential Create

> IR Admin · `/company/credentials/new` · id `company-credentials-new`
> 원본: `apps/ir-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Credential Create |
| 메뉴 위치 | 회사 > (목록에서 진입) |
| URL | `/company/credentials/new` |
| 화면 목적 | 받은 특허 · 인증을 사이트에 세운다. |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 사이트 특허 및 인증 화면 — 노출을 켜야 선다. |
| Figma 페이지 순번 | 55 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다 — `pnpm docs:capture` 로 뜹니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 기본 정보 | 코드 · 구분 · 이름 · 등록번호 · 발급 기관 · 취득일 · 노출 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 구분 고르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 이름·등록번호·발급 기관 넣기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 취득일 넣기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 노출 정하기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 등록 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 코드 | 가리키는 번호 | 읽기 전용 | 선택 | 저장할 때 매겨진다 | C-002 |
| 구분 | 목록에서 갈래로 쓰인다 | 선택 | 필수 | — | — |
| 이름 | 등록증에 적힌 이름 그대로 | 텍스트 | 필수 | — | 설비 신호의 표준 데이터 변환 방법 |
| 등록번호 | 밖에서 조회할 수 있는 값 | 텍스트 | 필수 | 고정폭으로 받는다 | 10-0000000 |
| 발급 기관 | 어디서 받았는지 | 텍스트 | 필수 | — | 특허청 |
| 취득일 | 받은 날 | 날짜 | 필수 | 미래는 막는다 | — |
| 사이트에 노출 | 끄면 사이트에서 사라진다 | 스위치 | 선택 | — | — |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 등록 | 번호와 취득일을 확인하고 확인 창을 연다 | 토스트로 알린다 | 확인이 필요한 칸을 토스트에 적는다 |

## 5. 사용자 시나리오

### 정상

- 구분 고르기 → 결과가 화면에 바로 반영된다.
- 이름·등록번호·발급 기관 넣기 → 결과가 화면에 바로 반영된다.
- 취득일 넣기 → 결과가 화면에 바로 반영된다.
- 노출 정하기 → 결과가 화면에 바로 반영된다.
- 등록 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 구분 고르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 이름·등록번호·발급 기관 넣기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 취득일 넣기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 노출 정하기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 등록 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 등록번호는 비운 채 저장할 수 없다 — 확인되지 않는 주장을 회사 홈페이지에 세우는 것은 그 자체로 위험한 일이다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 취득일이 미래면 막는다. 사이트는 날짜를 따지지 않고 그대로 세우므로 심사 중인 것이 받은 것으로 실린다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 오늘은 화면 밖에서 받는다 — 조각 안에서 읽으면 서버와 브라우저 값이 어긋난다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

- 등록번호가 비면 막는다.
- 취득일이 오늘보다 뒤면 막는다.

### 화면 정책

- 번호 입력 칸도 목록과 같은 고정폭 글꼴을 쓴다 — 자릿수가 눈으로 맞아야 들고 온 번호와 견줄 수 있다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 등록증 파일 올리기.

## 관련 문서

- [화면 캡처](/docs/page-view/company-credentials-new) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
