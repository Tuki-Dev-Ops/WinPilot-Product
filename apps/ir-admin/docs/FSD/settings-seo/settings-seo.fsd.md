# SEO

> IR Admin · `/settings/seo` · id `settings-seo`
> 원본: `apps/ir-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | SEO |
| 메뉴 위치 | 설정 > SEO 정보 |
| URL | `/settings/seo` |
| 화면 목적 | 검색 결과와 링크 공유 시 표시되는 정보를 관리하는 설정 페이지입니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 검색 엔진 검색 결과 · 메신저 링크 공유 시 표시되는 카드 |
| Figma 페이지 순번 | 91 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 검색 결과 정보 입력 영역 | 제목 · 설명 · 대표 URL | — |
| 공유 카드 정보 입력 영역 | 메신저 공유 시 표시되는 이미지 | — |
| 저장 확인 모달 | 검색 결과 반영 내용을 요약 표시 | 저장 버튼을 선택했을 때 |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 처리 | 결과 |
|---|---|---|---|
| 제목 및 설명 등록 기능 제공 | 제목 및 설명 입력 | 입력값을 검증한 뒤 신규 항목으로 저장한다 | 저장 결과를 안내하고 목록 화면으로 이동한다 |
| 대표 URL 등록 기능 제공 | 대표 URL 입력 | 입력값을 검증한 뒤 신규 항목으로 저장한다 | 저장 결과를 안내하고 목록 화면으로 이동한다 |
| 공유 이미지 경 등록 기능 제공 | 공유 이미지 경로 입력 | 입력값을 검증한 뒤 신규 항목으로 저장한다 | 저장 결과를 안내하고 목록 화면으로 이동한다 |
| 저장 기능 제공 | 저장 | 확인 모달을 표시합니다 | 저장 완료를 안내합니다 |

### 데이터 항목

| 항목 | 설명 | 입력 형태 | 필수 | 형식·제한 | 예시 |
|---|---|---|---|---|---|
| 제목 | 검색 결과에 표시되는 제목 | 텍스트 | 필수 | 길이 초과 시 절삭됩니다. 잔여 글자 수를 표시 | — |
| 설명 | 제목 하단에 표시되는 설명 | 여러 줄 | 필수 | 길이 초과 시 절삭됩니다 | — |
| 대표 URL | 동일 화면이 여러 URL 로 접근될 때의 기준 주소 | 텍스트 | 선택 | — | — |
| 공유 이미지 경로 | 미입력 시 링크 공유가 텍스트로만 표시됨 | 텍스트 | 선택 | — | — |

## 4. 버튼 및 이벤트

| 버튼 | 눌렀을 때 | 성공 | 실패 |
|---|---|---|---|
| 저장 | 확인 모달을 표시합니다 | 저장 완료를 안내합니다 | — |

## 5. 사용자 시나리오

### 정상

- 제목 및 설명 입력 → 결과가 화면에 바로 반영된다.
- 대표 URL 입력 → 결과가 화면에 바로 반영된다.
- 공유 이미지 경로 입력 → 결과가 화면에 바로 반영된다.
- 저장 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 제목 및 설명 입력 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 대표 URL 입력 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 공유 이미지 경로 입력 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 저장 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 글자 수를 표시하되 초과 시 저장을 차단하지 않습니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 공유 이미지를 미입력하면 링크 공유 시 텍스트만 표시된다고 안내합니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 저장 시 확인 모달을 표시합니다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 글자 수를 입력 중 표시합니다. 절삭 결과는 검색 결과에 표시되기 전까지 확인할 수 없습니다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 검색 결과 미리보기.

## 관련 문서

- [화면 캡처](/docs/page-view/settings-seo) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
