# Problem & Approach

> IR Admin · `/solutions` · id `solutions`
> 원본: `apps/ir-admin/lib/screen-specs.ts` · 생성: `pnpm docs:build`
> 이 파일은 **생성물**이다. 고칠 것은 원본이고, 여기서 고치면 다음 생성 때 지워진다.

## 1. 기능 명칭

| 항목 | 내용 |
|---|---|
| 화면명 | Problem & Approach |
| 메뉴 위치 | 메뉴 밖 |
| URL | `/solutions` |
| 화면 목적 | 제품 넷이 **어떤 문제를 어떻게 푸는지**가 채워졌는지 살핍니다. |
| 배경 | — |
| 기대 효과 | 이 화면이 없으면 위 목적을 다른 화면이 대신 떠안게 되고, 그 화면은 하는 일이 둘이 된다. |
| 고객 화면 연동 | 사이트 제품 상세 `/solutions/*` — 문제와 해법 문단이 이 값이다. |
| Figma 페이지 순번 | 40 |

여기서 정한 값이 나타나는 고객 화면이다. 이 화면이 원본이고, 고객 화면은 받아 그린다.

## 2. 기능 뷰

_캡처가 아직 없습니다. `pnpm docs:capture` 를 실행하면 채워집니다._

### 화면 구성

| 영역 | 용도 | 표시 조건 |
|---|---|---|
| 목록 툴바 | 검색 · 상태 | — |
| 표 | 한 줄이 제품 하나 — 구성 층과 절차가 찼는지 | — |

## 3. 기능 명세

| 기능 | 사용자 동작 | 시스템 동작 |
|---|---|---|
| 이름·문제로 검색 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 상태로 거르기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |
| 줄을 눌러 상세로 들어가기 | 해당 요소를 누르거나 값을 넣는다 | 조건을 확인하고 결과를 화면에 반영한다 |

### 데이터 항목

_입력 항목이 없는 읽기 전용 화면입니다._

## 4. 버튼 및 이벤트

_누르는 요소가 링크뿐입니다 — 누르면 해당 주소로 이동합니다._

## 5. 사용자 시나리오

### 정상

- 이름·문제로 검색 → 결과가 화면에 바로 반영된다.
- 상태로 거르기 → 결과가 화면에 바로 반영된다.
- 줄을 눌러 상세로 들어가기 → 결과가 화면에 바로 반영된다.

### 인수 조건

- **Given** 이 화면을 열었을 때 **When** 이름·문제로 검색 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 상태로 거르기 **Then** 그 결과가 화면에 반영된다.
- **Given** 이 화면을 열었을 때 **When** 줄을 눌러 상세로 들어가기 **Then** 그 결과가 화면에 반영된다.

## 6. 예외처리

| 발생 조건 | 사용자 안내 | 화면 처리 |
|---|---|---|
| 제품 목록과 같은 값을 보되 다른 칸을 보여 준다 — 여기서 확인할 것은 상세 화면이 채워졌는지다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |
| 등록 단추가 없다. 상세 화면이 코드로 짜여 있다. | 무엇이 왜 막혔는지 그 자리에서 알린다 | 입력값과 스크롤 자리를 지킨 채 안내만 더한다 |

## 7. 검증 규칙

_N/A — 이 화면에는 해당되지 않습니다._

### 화면 정책

- 갈래 이름이 `솔루션` 이 아닌 이유: 사이트에서 SOLUTION 은 **서비스 둘**을 뜻하게 되었다. 같은 말로 여기에 제품 넷을 세우면 서비스 문구를 고치러 온 사람이 제품 넷을 보게 된다.
- 주소는 `/solutions` 그대로다 — 운영자가 메뉴로만 여는 자리라 바꿔 얻는 것이 없고, 바꾸면 즐겨찾기와 캡처 명세가 함께 끊긴다.
- 구성 층과 절차가 비어 있으면 그 상세 화면은 문단 둘로 끝나고, 검토하러 온 사람은 거기서 문의 대신 창을 닫는다.

## 8. 화면 상태

| 상태 | 화면 |
|---|---|
| 최초 | 값이 오기 전의 자리(뼈대)를 그려 둔다 — 자리를 비우면 화면이 튄다 |
| 불러온 뒤 | 정상 화면 |
| 비어 있음 | 목록 자리를 비우지 않고 왜 비었는지 한 줄로 알린다 |
| 오류 | 같은 자리에 안내와 다시 시도할 길을 둔다 |

### UX 정책

- 조건을 채우지 못한 단추는 **잠근다.** 눌러 놓고 다음 화면에서 막으면 무엇이 잘못됐는지 늦게 안다.
- 알림은 화면 하단 정중앙 토스트 한 곳에서만 띄운다.
- 되돌릴 수 없는 일(삭제·취소)은 확인을 한 번 묻는다.
- 키보드만으로 모든 요소에 닿을 수 있고, 포커스가 어디 있는지 눈에 보인다.
- ESC 로 열린 것(모달·서랍)을 닫는다.

### 향후 확장

- 비어 있는 상세를 먼저 세워 보여 주기.

## 관련 문서

- [화면 캡처](/docs/page-view/solutions) · [비기능 명세](/docs/nfs) · [IA](/docs/ia)
