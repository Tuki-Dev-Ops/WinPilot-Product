/**
 * 확인 창 — 이제 `@winpilot/ui` 의 `ConfirmModal` 이다.
 *
 * 왜 저장에도 세우는지, 왜 `elevated` 인지는 올라간 쪽 머리말에 있다.
 *
 * 이 겹을 남겨 두는 이유는 `IrPanel` 과 같다.
 */
export { ConfirmModal as IrConfirmModal } from '@winpilot/ui';
