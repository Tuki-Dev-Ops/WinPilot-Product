/**
 * 대한민국 시 · 도 경계선 — **여러 앱이 나눠 쓴다.**
 *
 * ## 왜 패키지로 올렸나
 * 처음에는 IR 어드민 안에만 있었다(`apps/ir-admin/lib/geo`). F&B 클라이언트의 매장 찾기가 같은
 * 경계선을 쓰게 되면서 올렸다 — 앱마다 두면 216KB 짜리 좌표 파일이 두 벌이 되기 때문이다.
 *
 * **지금은 다시 IR 어드민만 쓴다.** 매장 찾기는 카카오 지도로 갈아탔다 — 거기서는 지도가 곧
 * 답이라 길과 건물이 필요했고, 행정 경계만으로는 "여기서 어떻게 가지" 에 답할 수 없었다.
 *
 * 패키지를 되돌리지 않는 이유: 쓰는 앱이 하나로 줄었다고 앱 안으로 다시 넣으면, 다음에 또
 * 필요해질 때 같은 일을 반복한다. 자리는 그대로 두고 **쓰지 않는 층만** 걷어냈다(시 · 군 · 구 ·
 * 읍 · 면 · 동과 그 원본 파일 2MB).
 *
 * ## 브라우저로는 좌표가 내려가지 않는다
 * `KOREA_SHAPES` 는 **모듈을 불러오는 순간 한 번** 계산된다. 서버 컴포넌트가 그것을 읽어
 * `<path d>` 문자열만 화면에 넘기므로, `d3-geo` 와 216KB 짜리 topojson 은 서버에 남는다.
 *
 * 쓰는 쪽은 그 사실을 알 필요가 없다 — 다만 **클라이언트 컴포넌트에서 직접 import 하지 않는
 * 것**만 지키면 된다. 그러면 번들에 그대로 딸려 들어간다.
 */
export { KOREA_BOX, KOREA_SHAPES, type KoreaShape } from './korea';
