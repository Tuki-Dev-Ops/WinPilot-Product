/**
 * 프로젝트의 목적과 배경 — **일곱 앱이 같은 글을 그린다.**
 *
 * ## 왜 공유 패키지에 있나
 * 이 글은 저장소 전체에 대한 것이라 앱마다 달라질 이유가 없다. 그런데 앱마다 한 벌씩 두면
 * 일곱 벌이 되고, 그때부터는 **아무도 고치지 않는다** — 같은 실수를 `/docs/prompt` 에서 이미
 * 했다(그쪽 머리말에 여든한 줄이 일곱 벌이던 이야기가 있다).
 *
 * ## 적지 않은 것
 * 누가 발주했는지, 언제까지인지, 얼마짜리인지는 적지 않았다. **저장소에서 확인할 수 없는
 * 것이라서다.** 문서가 코드에서 확인되지 않는 말을 담기 시작하면, 읽는 사람은 어디까지가
 * 사실인지 몰라 전부를 의심하게 된다.
 */
export const PROJECT_PURPOSE = `# 프로젝트 목적과 배경

> 이 글은 \`packages/docs/src/purpose.ts\` 한 곳에 있고, 일곱 앱이 같은 내용을 보여 줍니다.
> 저장소에서 확인할 수 있는 것만 담았습니다 — 발주와 일정, 비용처럼 코드에 남지 않는 것은
> 적지 않았습니다.

## 1. 무엇을 만들고 있는가

웹 화면 일곱 벌을 만들고 있습니다. 고객이 보는 사이트가 셋, 그 사이트의 내용을 고치는
운영 콘솔이 셋, 그리고 그 셋을 함께 들여다보는 사내 콘솔이 하나입니다.

| 짝 | 고객 화면 | 운영 콘솔 |
|---|---|---|
| B2C | 쇼핑몰 (\`b2c-client-a\` · 3310) | \`b2c-admin\` · 3301 |
| IR | 회사 홈페이지 (\`ir-client-a\` · 3304) | \`ir-admin\` · 3303 |
| F&B | 외식 브랜드 사이트 (\`fnb-client-a\` · 3305) | \`fnb-admin\` · 3306 |
| 사내 | — | \`internal-admin\` · 3302 |

폴더 이름 끝의 \`-a\` 는 템플릿 A 를 뜻합니다. 같은 업종의 사이트를 다른 배치로 한 벌 더
만들 수 있도록 이름에 자리를 열어 둔 것이고, 지금은 업종마다 한 벌씩 있습니다.

## 2. 왜 이렇게 만들고 있는가

### 서버를 두지 않았습니다

API 도 데이터베이스도 없습니다. 화면에 나오는 값은 모두 \`packages/store\` 한 곳에 있고,
운영 콘솔과 고객 화면이 그 한 곳을 함께 읽습니다.

임시방편처럼 보이지만 의도가 있습니다. 두 화면이 각자 자기 데이터를 들고 있으면 머지않아
"콘솔에서 본 값과 사이트에 뜬 값이 다르다" 는 상황이 오고, 그때부터는 어느 쪽이 맞는지
아무도 답할 수 없게 됩니다. 원본을 하나로 두면 그 다툼 자체가 생기지 않습니다. 나중에 서버가
붙더라도 바뀌는 것은 이 패키지가 값을 어디서 받아 오는가뿐이고, 화면은 그대로 둘 수 있습니다.

### 디자인과 코드가 갈라지지 않게 했습니다

색과 간격, 글자 크기를 화면마다 적어 두면 두어 달 뒤에는 같은 회색이 네 가지가 되어 있습니다.
그래서 이 값들을 \`packages/tokens/theme.css\` 한 파일에 모으고, 일곱 앱이 그것만 바라보게
했습니다.

그림도 같은 이유로 다룹니다. 아이콘과 도표를 이미지 파일 대신 인라인 SVG 로 그리는 것은,
화면을 다시 Figma 로 옮길 때 비트맵은 벡터로 되살아나지 않기 때문입니다. 로고만 예외로 받은
파일을 그대로 쓰는데, 눈으로 보고 다시 그린 로고는 그 회사의 표장이 아니라 비슷하게 생긴
다른 그림이기 때문입니다.

### 문서가 화면을 따라오는 일을 사람 손에 맡기지 않았습니다

기능 명세와 비기능 명세는 손으로 쓰지 않습니다. 화면마다 \`lib/screen-specs.ts\` 에 한 항목을
적어 두면 \`pnpm docs:build\` 가 문서를 펼쳐 냅니다. 생성된 문서를 직접 고치면 다음 실행 때
지워지는데, 고칠 곳이 원본 한 군데뿐이어야 두 문서가 서로 다른 말을 하지 않기 때문입니다.

여기에 더해, 어긋남의 종류마다 검사를 따로 두었습니다.

| 검사 | 무엇을 보는가 |
|---|---|
| \`spec:check\` | 이름과 주소, 매니페스트가 레지스트리와 맞는지 |
| \`sync:check\` | 레지스트리에 적힌 컴포넌트 이름이 실제 파일에도 그대로인지 |
| \`docs:check\` | 화면은 있는데 문서가 비어 있는 자리가 있는지 |
| \`bind:check\` | 화면이 적어 둔 값의 출처가 아직 남아 있는지 |
| \`overflow:check\` | 네 가지 너비에서 가로로 넘치는 곳이 있는지 |
| \`weight:check\` | 화면 하나가 실제로 몇 바이트를 내려받는지 |

검사를 여럿 두는 것은 하나가 나머지를 대신하지 못하기 때문입니다. 실제로 운영 콘솔에서 화면
열 개를 지웠을 때 고객 화면 열일곱 곳이 이미 없어진 메뉴를 계속 가리키고 있었는데, 그 시점에
나머지 검사는 모두 통과 상태였습니다. \`bind:check\` 는 그 일을 겪고 나서 만들었습니다.

## 3. 무엇은 하지 않는가

- **실제로 서비스하는 단계가 아닙니다.** 결제와 로그인은 화면까지만 있고, 입력한 값은 어디로도
  전송되지 않습니다. 양식 화면의 명세마다 그 사실을 한 줄씩 적어 두는 것은, 적지 않으면 이
  문서를 보고 개발하는 분이 존재하지 않는 접수 경로를 찾게 되기 때문입니다.
- **디자인 시안이 아닙니다.** 화면은 실제로 동작하는 Next 애플리케이션이고, 이 문서 역시 그
  애플리케이션 안의 실제 주소(\`/docs/**\`)입니다. 문서를 따로 배포하거나 위키에 올리지 않는
  것은, 화면과 문서가 같은 저장소에서 함께 바뀌어야 서로 어긋나지 않기 때문입니다.
- **특정 한 회사의 사이트가 아닙니다.** 세 업종은 각각 템플릿이고, 회사 이름과 상품, 메뉴는
  모두 \`packages/store\` 가 들고 있는 값입니다.

## 4. 화면 하나하나의 목적과 배경

이 글은 저장소 전체를 놓고 쓴 것입니다. 화면 하나가 왜 있고 그전에는 무엇이 불편했는지는
기능 명세서(FSD) 1절에 두 줄로 적어 두었습니다.

- **목적** — 이 화면이 지금 무엇을 하는가
- **배경** — 이 화면이 없던 때 무엇이 불편했는가

둘을 한 칸에 묶지 않은 것은, 그렇게 하면 목적 문장이 길어지면서 정작 "무엇을 하는 화면인가"
가 흐려지기 때문입니다. 약관이나 개인정보 처리방침처럼 법이 요구해서 존재하는 화면은 배경을
비워 두었습니다. 억지로 채우면 사실이 아닌 이야기가 문서에 남습니다.
`;
